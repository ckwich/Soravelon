---
phase: 01-patrol-commands-and-flight-paths
plan: 06
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - tests/test_patrol_system.py
  - tests/test_trigger_system.py
  - tests/test_command_prefix.py
  - tests/test_flight_system.py
autonomous: true
requirements:
  - IWA-01
  - IWA-02
  - IWA-03
  - IWA-04
  - IWA-05
  - IWA-06
  - IWA-07
  - IWA-08
  - FLT-01
  - FLT-02
  - FLT-03
  - FLT-04
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "All 4 test files pass: pytest tests/test_patrol_system.py tests/test_trigger_system.py tests/test_command_prefix.py tests/test_flight_system.py exits 0"
    - "Patrol BFS pathfinding finds correct path between connected rooms"
    - "Patrol interrupt modes (resume/reset_to_start/abandon) behave correctly after combat"
    - "Triggers with once_per_character do not re-fire for same character"
    - "Cooldown triggers respect time constraints"
    - "Prefix resolution returns error on ambiguous input, resolves on unique match"
    - "Aliases with $1/$2/$*/$@ tokens expand correctly"
    - "Aliases capped at 3 chained commands"
    - "Flight fare applies Consortium standing discounts correctly"
    - "book_flight fails when destination not in discovered_flight_points"
    - "FlightRegistry BFS finds multi-leg routes correctly"
  artifacts:
    - path: "tests/test_patrol_system.py"
      provides: "PatrolScript, find_path(), check_patrol_encounter() test coverage"
    - path: "tests/test_trigger_system.py"
      provides: "trigger_engine fire_triggers(), once-per, cooldown test coverage"
    - path: "tests/test_command_prefix.py"
      provides: "command_preprocessor resolve_prefix, expand_alias, preprocess_input tests"
    - path: "tests/test_flight_system.py"
      provides: "FlightRegistry, fare_for_route, book_flight, FlightScript tests"
  key_links:
    - from: "tests/"
      to: "all Phase 1 modules"
      via: "direct imports of each module under test"
      pattern: "from world.patrol_engine import"
---

<objective>
Write the test suite for all Phase 1 systems. Four test files covering the patrol engine, trigger system, command preprocessor, and flight system. Tests use Evennia's EvenniaTestCase pattern established in the existing test suite.

Purpose: Executable verification that all 17 Phase 1 requirements are satisfied. Tests are the acceptance criteria for this phase.
Output: tests/test_patrol_system.py, tests/test_trigger_system.py, tests/test_command_prefix.py, tests/test_flight_system.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-patrol-commands-and-flight-paths/01-CONTEXT.md

<interfaces>
<!-- Existing test patterns from the project. -->

From tests/test_world_state.py (representative test file — read this before writing tests):
```python
# Pattern: EvenniaTestCase, create_object, mock-style setup
from evennia.utils.test_resources import EvenniaTestCase
from typeclasses.characters import Character

class TestWorldState(EvenniaTestCase):
    def setUp(self):
        super().setUp()
        self.char = create_object(Character, key="TestChar")
        # set up db attrs directly

    def test_something(self):
        result = some_function(self.char)
        self.assertEqual(result, expected)
```

From tests/test_typeclasses.py (room/mob creation pattern):
```python
from evennia.utils.test_resources import EvenniaTestCase
from evennia import create_object
from typeclasses.rooms import SoravelonRoom
from typeclasses.mobs import SoravelonMob
```

Key constraint (D-22): Tests create mobs with create_object(SoravelonMob) and attach PatrolScript directly — no zone builder needed for tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patrol + Trigger Tests</name>
  <files>
    tests/test_patrol_system.py
    tests/test_trigger_system.py
  </files>
  <read_first>
    - tests/test_world_state.py (full file — establishes test patterns for this project)
    - tests/test_typeclasses.py (full file — room and mob creation patterns)
    - world/patrol_engine.py (find_path, next_patrol_step, check_patrol_encounter signatures)
    - world/scripts/patrol_script.py (PatrolScript db attrs and on_combat_end method)
    - world/trigger_engine.py (fire_triggers signature, once_per and cooldown behavior)
    - world/action_vocabulary.py (execute_action — for testing trigger→action dispatch)
  </read_first>
  <action>
**tests/test_patrol_system.py**:

Test classes:

1. `TestFindPath(EvenniaTestCase)`:
   - `setUp`: Create 3 rooms in a line (A→B→B→C exits) within same zone_id="test_zone"
   - `test_same_room`: `find_path(A, A)` == `[A]`
   - `test_two_rooms`: `find_path(A, C)` returns `[A, B, C]` (or `[A, C]` if direct)
   - `test_unreachable`: `find_path(A, D)` where D has different zone_id == `[]`
   - `test_max_depth`: `find_path(A, Z)` with max_depth=1 on a longer path == `[]`

2. `TestNextPatrolStep(EvenniaTestCase)`:
   - `setUp`: Create 3 rooms; store their ids in route_ids list
   - `test_advance`: next_patrol_step at index 0 returns room[1], index 1
   - `test_wrap`: next_patrol_step at last index returns room[0], index 0
   - `test_empty_route`: next_patrol_step with [] returns (None, 0)

3. `TestCheckPatrolEncounter(EvenniaTestCase)`:
   - `setUp`: Create mob + room + character in room; `get_mob_behavior` will be mocked
   - `test_combat_disabled`: mob.db.combat_enabled=False → returns False regardless
   - `test_no_players`: empty room → returns False
   - `test_aggressive_behavior`: mock get_mob_behavior to return "aggressive" → returns True
   - `test_passive_behavior`: mock returns "passive" → returns False

4. `TestPatrolScriptInterruptModes(EvenniaTestCase)`:
   - `setUp`: Create mob; attach PatrolScript directly (D-22); set route_ids, route_index=1
   - `test_resume`: `on_combat_end("win")` with interrupt_mode="resume" → route_index unchanged, interrupted=False
   - `test_reset`: `on_combat_end("win")` with interrupt_mode="reset_to_start" → route_index=0
   - `test_abandon`: `on_combat_end("win")` with interrupt_mode="abandon" → script is stopped

**tests/test_trigger_system.py**:

Test classes:

1. `TestFireTriggers(EvenniaTestCase)`:
   - `setUp`: Create room + character; room.db.triggers=[]
   - `test_no_triggers`: fire_triggers(room, "on_enter", char) with empty list → no error
   - `test_wrong_event`: trigger with event="on_exit" not fired on "on_enter"
   - `test_non_character`: fire_triggers(room, "on_enter", mob) where mob has no .account → silent return, no fire
   - `test_fires_in_order`: 2 triggers on same event with distinct echo actions → both fire, in order

2. `TestOncePerCharacter(EvenniaTestCase)`:
   - `setUp`: Create room + character; add trigger with once_per_character=True, trigger_id="t01"
   - `test_fires_first_time`: trigger fires; character.db.fired_triggers contains "t01"
   - `test_skips_second_time`: fire_triggers called again → trigger does NOT fire (check action side effect)

3. `TestCooldown(EvenniaTestCase)`:
   - Use mock/patch to control datetime.utcnow() for deterministic testing
   - `test_fires_initially`: trigger with cooldown_seconds=60 fires on first call
   - `test_blocked_in_window`: second call within 60s → trigger does NOT fire
   - `test_fires_after_cooldown`: second call after 60s (mocked time) → fires again

4. `TestDepthLimit(EvenniaTestCase)`:
   - `test_action_depth_limit`: call execute_action with _depth=3 → returns (False, "Trigger chain depth limit reached.")
   - `test_stub_actions`: execute_action({"action_type": "spawn_mob"}, {}) → returns (False, msg)
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -m pytest tests/test_patrol_system.py tests/test_trigger_system.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/test_patrol_system.py exists with 4 test classes (TestFindPath, TestNextPatrolStep, TestCheckPatrolEncounter, TestPatrolScriptInterruptModes)
    - tests/test_trigger_system.py exists with 4 test classes (TestFireTriggers, TestOncePerCharacter, TestCooldown, TestDepthLimit)
    - All tests in both files pass (exit 0)
    - Tests use create_object() and direct db attribute setting — no AreaBuilder required (D-22)
  </done>
</task>

<task type="auto">
  <name>Task 2: Command Prefix + Flight System Tests</name>
  <files>
    tests/test_command_prefix.py
    tests/test_flight_system.py
  </files>
  <read_first>
    - tests/test_banking.py (full file — banking test patterns for flight fare tests)
    - world/command_preprocessor.py (resolve_prefix, expand_alias, preprocess_input)
    - world/flight_registry.py (FlightRegistry API — use FlightRegistry.clear() in tearDown)
    - world/flight_engine.py (fare_for_route, book_flight — need banking mock or real account setup)
  </read_first>
  <action>
**tests/test_command_prefix.py**:

Test classes (pure unit tests — no Evennia DB needed for most):

1. `TestResolvePrefix`:
   - `test_exact_match`: resolve_prefix("north", ["north", "attack"]) == ("north", [])
   - `test_unique_prefix`: resolve_prefix("no", ["north", "attack"]) == ("north", [])
   - `test_ambiguous`: resolve_prefix("att", ["attack", "attune"]) == (None, ["attack", "attune"])
   - `test_ambiguous_sorted`: result matches list is sorted alphabetically
   - `test_no_match`: resolve_prefix("xyz", ["north"]) == (None, [])
   - `test_case_insensitive`: resolve_prefix("NO", ["north"]) == ("north", [])

2. `TestExpandAlias(EvenniaTestCase)`:
   - `setUp`: Create character; set db.aliases={"kill": "attack $1", "buff": "cast shield; cast haste; cast blessing"}
   - `test_single_expand`: expand_alias(char, "kill", "orc") == ["attack orc"]
   - `test_chain_expand`: expand_alias(char, "buff", "") == ["cast shield", "cast haste", "cast blessing"]
   - `test_unknown_alias`: expand_alias(char, "unknown", "") is None
   - `test_token_dollar_star`: set alias "x"="say $*"; expand_alias(char, "x", "hello world") == ["say hello world"]
   - `test_cap_at_three`: set alias "y"="a; b; c; d"; expand_alias returns only ["a", "b", "c"]
   - `test_no_aliases_attr`: character with db.aliases=None → expand_alias returns None gracefully

3. `TestPreprocessInput(EvenniaTestCase)`:
   - Mock _get_all_cmd_keys to return ["attack", "attune", "north", "look"]
   - `test_exact_system_cmd`: preprocess_input(char, "look") == "look" (no change)
   - `test_unique_prefix`: preprocess_input(char, "no") == "north"
   - `test_ambiguous_sends_error`: preprocess_input(char, "att") returns None; char.msg called with "Did you mean:"
   - `test_alias_expands`: char has alias "k"="attack"; preprocess_input(char, "k orc") == ["attack orc"]
   - `test_alias_blocked_by_system_cmd`: char has alias "look"="look at ceiling"; preprocess_input(char, "look") returns "look" (system cmd wins)

**tests/test_flight_system.py**:

Test classes:

1. `TestFlightRegistry(EvenniaTestCase)`:
   - `setUp`: Call FlightRegistry.clear(); register 3 points A→B→C with routes A-B and B-C
   - `tearDown`: FlightRegistry.clear()
   - `test_register_point`: FlightRegistry.get_point("A") returns dict with room_id
   - `test_register_route_bidirectional`: register A→B; FlightRegistry.get_route("A","B") and get_route("B","A") both return dict
   - `test_find_direct_route`: find_route_legs("A","B") == [("A","B")]
   - `test_find_multi_leg`: find_route_legs("A","C") == [("A","B"), ("B","C")]
   - `test_no_route`: find_route_legs("A","D") where D unregistered == []

2. `TestFareCalculation(EvenniaTestCase)`:
   - `setUp`: FlightRegistry.clear(); register 2-leg route (A→B base_fare=100, B→C base_fare=100); set up character
   - Mock `get_standing` to return specific values
   - `test_no_discount`: standing=10 → fare=200 (0% discount)
   - `test_10_percent`: standing=30 → fare=180
   - `test_20_percent`: standing=60 → fare=160
   - `test_30_percent`: standing=80 → fare=140
   - `test_single_leg`: find_route_legs("A","B") → fare=100 at standing=0

3. `TestBookFlight(EvenniaTestCase)`:
   - `setUp`: FlightRegistry.clear(); register points and route; create character with balance and discovered set
   - `test_success`: book_flight(char, "A", "B") returns (True, ""); character has FlightScript; balance reduced
   - `test_undiscovered`: B not in character.db.discovered_flight_points → (False, "not discovered" msg)
   - `test_insufficient_funds`: character balance=0 → (False, "Insufficient funds" msg)
   - `test_no_route`: no route registered → (False, "No route found" msg)
   - `test_fare_not_double_deducted`: create script; simulate reload — check db.fare_paid guard

4. `TestFlightDiscovery(EvenniaTestCase)`:
   - `setUp`: Create room with db.flight_point_id="point_A"; character outside room
   - `test_auto_discover_on_enter`: manually call room.at_object_receive(char, None); char.db.discovered_flight_points contains "point_A"
   - `test_no_duplicate_message`: second at_object_receive call → msg NOT sent again (set already contains point_A)
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -m pytest tests/test_command_prefix.py tests/test_flight_system.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/test_command_prefix.py exists with 3 test classes (TestResolvePrefix, TestExpandAlias, TestPreprocessInput)
    - tests/test_flight_system.py exists with 4 test classes (TestFlightRegistry, TestFareCalculation, TestBookFlight, TestFlightDiscovery)
    - All tests in both files pass (exit 0)
    - FlightRegistry.clear() used in setUp/tearDown to isolate test state
  </done>
</task>

</tasks>

<verification>
Full test suite run:
```
cd C:/Dev/Evennia/soravelon
python -m pytest tests/test_patrol_system.py tests/test_trigger_system.py tests/test_command_prefix.py tests/test_flight_system.py -v 2>&1 | tail -30
```

All 4 files must exit 0 with no FAILED or ERROR lines.

Existing tests must still pass:
```
python -m pytest tests/ -v 2>&1 | tail -10
```
</verification>

<success_criteria>
- tests/test_patrol_system.py: covers find_path BFS, next_patrol_step wrap, check_patrol_encounter, all 3 interrupt modes
- tests/test_trigger_system.py: covers once_per_character, cooldown gating, depth limit, non-character guard
- tests/test_command_prefix.py: covers resolve_prefix (exact/unique/ambiguous/no match), expand_alias (tokens, cap), preprocess_input (system cmd wins over alias)
- tests/test_flight_system.py: covers FlightRegistry BFS, all 4 fare discount tiers, book_flight validation paths, auto-discovery on room enter
- All 4 test files pass with python -m pytest; no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-patrol-commands-and-flight-paths/01-06-SUMMARY.md` following the summary template.
</output>
