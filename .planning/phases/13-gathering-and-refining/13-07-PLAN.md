---
phase: 13-gathering-and-refining
plan: 07
type: execute
wave: 5
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06]
files_modified:
  - tests/test_gathering.py
  - tests/test_fishing.py
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8]

must_haves:
  truths:
    - "All 8 success criteria have automated test coverage"
    - "Tests pass with evennia test runner"
    - "Existing test suite (tests/) still passes — no regressions"
  artifacts:
    - path: "tests/test_gathering.py"
      provides: "Tests for material registry, gathering engine, commands, processing, prospect, tools, mob loot"
      min_lines: 200
    - path: "tests/test_fishing.py"
      provides: "Tests for fishing active and idle modes"
      min_lines: 80
  key_links:
    - from: "tests/test_gathering.py"
      to: "world/gathering_engine.py"
      via: "tests spawn_gathering_pool, gather_from_node, prospect_scan"
      pattern: "from world.gathering_engine import"
    - from: "tests/test_gathering.py"
      to: "world/material_definitions.py"
      via: "tests MATERIAL_REGISTRY structure and completeness"
      pattern: "from world.material_definitions import"
---

<objective>
Create comprehensive test suites covering all 8 success criteria for Phase 13, using the project's established testing patterns.

Purpose: Validates that gathering nodes spawn correctly, commands work with tools and skills, processing recipes convert properly, fishing modes function, prospect scans work, and mob loot feeds the pipeline. The test suite is the phase gate for /gsd:verify-work.
Output: tests/test_gathering.py, tests/test_fishing.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-gathering-and-refining/13-CONTEXT.md
@.planning/phases/13-gathering-and-refining/13-RESEARCH.md
@.planning/phases/13-gathering-and-refining/13-VALIDATION.md

@tests/test_crafting.py
@world/gathering_engine.py
@world/material_definitions.py
@commands/cmd_gathering.py
@commands/cmd_fishing.py
@commands/cmd_prospect.py
@world/crafting_engine.py

<interfaces>
From world/gathering_engine.py:
```python
def spawn_gathering_pool(zone_obj, pool_def): ...
def deplete_node(node, pool_script): ...
def spawn_node_in_pool(pool_script, exclude_room_id=None): ...
def gather_from_node(character, node): -> (True, material_id) or (False, str)
def initialize_zone_gathering(zone_obj): ...
def prospect_scan(character, max_range): -> list[dict]
def get_butcher_yields(mob_key): -> list[dict]
class GatheringPoolScript: ...
```

From world/material_definitions.py:
```python
MATERIAL_REGISTRY = {...}
MATERIAL_TIERS = {1: "Common", ...}
GATHERING_CATEGORIES = {"ore": {...}, ...}
GATHER_DELAY_BY_TIER = {1: 4, ...}
VISIBILITY_THRESHOLDS = {"low": 0, "mid": 30, "high": 60}
TOOL_DURABILITY = {"pickaxe": {...}, ...}
```

From world/crafting_engine.py:
```python
def get_conversion_quantity(character, recipe): -> int or None
def calculate_processing_quality(character_skill, recipe_difficulty, raw_quality, has_station): -> str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_gathering.py covering SC-1 through SC-4, SC-6 through SC-8</name>
  <files>tests/test_gathering.py</files>
  <read_first>
    - tests/test_crafting.py (testing patterns — how to set up mock characters, items, rooms for crafting tests)
    - tests/test_area_builder.py (AreaBuilderTestBase pattern if available)
    - world/gathering_engine.py (all function signatures and class structure)
    - world/material_definitions.py (MATERIAL_REGISTRY structure to validate)
    - world/crafting_engine.py (get_conversion_quantity, calculate_processing_quality)
    - world/item_spawner.py (create_item_from_template — verify item_tag is set)
    - commands/cmd_gathering.py (_BaseGatherCmd, CmdButcher)
    - commands/cmd_prospect.py (CmdProspect, CmdRepair)
  </read_first>
  <action>
Create `tests/test_gathering.py` using the project's testing conventions. Use `unittest.TestCase` with `MagicMock` for pure-logic tests, and `EvenniaTestCase` for tests requiring DB objects. Check how existing tests (test_crafting.py) handle this split.

**Test classes:**

**TestMaterialRegistry (SC-4):**
- test_all_tiers_present: MATERIAL_TIERS has keys 1-5
- test_all_categories_present: GATHERING_CATEGORIES has all 6 categories
- test_registry_structure: Every MATERIAL_REGISTRY entry has required keys (display_name, category, tier, raw_form, processed_form, gathering_skill, processing_skill, processing_station, visibility)
- test_categories_match: Every material's category exists in GATHERING_CATEGORIES
- test_tiers_in_range: Every material's tier is 1-5
- test_visibility_values: Every material's visibility is "low", "mid", or "high"

**TestGatheringPoolSpawn (SC-1):**
- test_spawn_node_creates_gathering_node: Mock a pool_script with eligible_room_ids. Call spawn_node_in_pool. Verify a GatheringNode is created in an eligible room with correct db attrs.
- test_spawn_excludes_depleted_room: After depletion, next spawn should not go to last_depleted_room_id (when >1 eligible room)
- test_max_active_enforced: Pool with max_active=2 should not spawn a 3rd node
- test_gather_from_node_decrements: Call gather_from_node. Verify gathers_remaining decreases.
- test_gather_from_depleted_triggers_respawn: Gather until gathers_remaining hits 0. Verify deplete_node is called.

**TestGatherCommands (SC-2):**
- test_mine_requires_pickaxe: Mock character without pickaxe. CmdMine should fail with tool message.
- test_forage_needs_no_tool: CmdForage should not require a tool (required_tool is None)
- test_gather_delay_skill_reduction: At skill 100, delay should be 40% of base. At skill 0, delay should be 100% of base.
- test_gather_delay_minimum: Verify delay never goes below base * 0.4

**TestProcessingRecipes (SC-3):**
- test_conversion_ratio_low_skill: get_conversion_quantity with skill=10, recipe with thresholds [30,60,85] should return 3
- test_conversion_ratio_mid_skill: skill=45 should return 2
- test_conversion_ratio_high_skill: skill=90 should return 1
- test_conversion_ratio_none_for_regular: Regular recipe (no conversion_ratio field) should return None
- test_processing_quality_propagation: calculate_processing_quality with raw_quality="fine" and high skill should produce >= "standard"

**TestToolDurability (SC-6):**
- test_tool_has_durability_attr: Tool items should have db.durability set
- test_durability_decrements: After one gather, tool.db.durability should decrease by 1
- test_broken_tool_rejected: Tool with durability=0 should fail tool check
- test_repair_restores_durability: CmdRepair at workbench with smithing skill restores durability

**TestProspect (SC-7):**
- test_prospect_scan_finds_nodes: Set up rooms in a line with nodes. prospect_scan should find them with correct direction and distance.
- test_prospect_scan_stops_at_wall: No exit in direction = stop scanning that direction.
- test_prospect_scan_straight_line_only: Nodes around corners (not in straight line) should NOT be found.
- test_prospect_range_scales_with_skill: Higher skill = greater max_range.

**TestMobLootProcessing (SC-8):**
- test_item_spawner_sets_item_tag: create_item_from_template should set item_tag category tag.
- test_butcher_yields_default: get_butcher_yields("unknown_mob") should return default meat/bone.
- test_butcher_yields_specific: get_butcher_yields("boar") should return boar_hide material.

**TestSkillDefinitions:**
- test_new_skills_exist: SKILL_DEFINITIONS contains mining, woodcutting, skinning
- test_new_skills_structure: Each new skill has required keys (name, skill_type, domain_bonus, trainer_required_above, thresholds)

Each test should be concise (5-15 lines). Use MagicMock extensively for character, room, node objects in pure-logic tests. For tests that need actual DB objects, use EvenniaTestCase.

Import structure:
```python
import unittest
from unittest.mock import MagicMock, patch
```

For EvenniaTestCase tests (if needed):
```python
from evennia.utils.test_resources import EvenniaTestCase
```
  </action>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('tests/test_gathering.py').read()); classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]; print(f'{len(classes)} test classes: {classes}'); assert len(classes) >= 7"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_gathering.py contains >= 7 test classes covering SC-1, SC-2, SC-3, SC-4, SC-6, SC-7, SC-8
    - TestMaterialRegistry has >= 4 test methods validating MATERIAL_REGISTRY structure
    - TestGatheringPoolSpawn has >= 3 test methods for node spawning and depletion
    - TestGatherCommands has >= 3 test methods for tool checks and delay calculation
    - TestProcessingRecipes has >= 3 test methods for conversion ratios
    - TestToolDurability has >= 3 test methods for durability lifecycle
    - TestProspect has >= 3 test methods for straight-line scanning
    - TestMobLootProcessing has >= 2 test methods for item_tag and butcher yields
    - All tests use unittest.TestCase or EvenniaTestCase (not pytest fixtures)
    - File has >= 200 lines
  </acceptance_criteria>
  <done>Comprehensive test suite covering all non-fishing success criteria with 25+ individual test methods</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_fishing.py covering SC-5</name>
  <files>tests/test_fishing.py</files>
  <read_first>
    - commands/cmd_fishing.py (full file — CmdFish state machine, CmdReel)
    - tests/test_gathering.py (just created — match testing patterns)
    - tests/test_crafting.py (mock patterns for commands)
  </read_first>
  <action>
Create `tests/test_fishing.py` with:

**TestFishingStateMachine:**
- test_fish_sets_active_state: After "fish" command, character.ndb.fishing_state should have mode="active", phase="cast"
- test_fish_idle_sets_idle_state: After "fish idle" command, should have mode="idle"
- test_fish_stop_clears_state: After "fish stop", fishing_state should be None
- test_fish_requires_rod: Without fishing_rod in inventory, command should fail
- test_fish_requires_fish_spot: Without GatheringNode(node_type="fish") in room, should fail

**TestActiveFishing:**
- test_bite_event_changes_phase: After bite fires, phase should be "bite"
- test_reel_during_bite_catches: CmdReel during bite phase should produce a fish item
- test_miss_resets_to_cast: If reel window expires, should restart cast (not stop)

**TestIdleFishing:**
- test_idle_catch_produces_item: Idle catch should create a fish item
- test_idle_quality_penalty: Idle catch quality should be 1 tier lower than active
- test_idle_reschedules: After idle catch, another idle timer should be scheduled

**TestBaitSystem:**
- test_bait_consumed_on_catch: Bait item should be deleted after catch
- test_bait_improves_quality: Catch with bait should have +1 quality tier
- test_no_bait_still_works: Fishing without bait should succeed normally

Use MagicMock for character, room, GatheringNode, bait items. Mock `evennia.utils.delay` to capture callbacks and call them synchronously in tests. Mock `gather_from_node` to return `(True, "river_trout")`.

```python
import unittest
from unittest.mock import MagicMock, patch, PropertyMock

# Mock evennia.utils.delay to capture callbacks
```
  </action>
  <verify>
    <automated>python -c "import ast; tree = ast.parse(open('tests/test_fishing.py').read()); classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]; methods = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]; print(f'{len(classes)} classes, {len(methods)} tests'); assert len(methods) >= 10"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fishing.py contains >= 3 test classes (state machine, active, idle, bait)
    - >= 10 individual test methods
    - Tests cover: active cast/bite/reel cycle, idle periodic catches, stop command, bait consumption, quality differences
    - Tests use MagicMock for character and node objects
    - Tests mock evennia.utils.delay to test callbacks synchronously
    - File has >= 80 lines
  </acceptance_criteria>
  <done>Fishing test suite with 10+ tests covering active mini-game, idle mode, bait system, and state management</done>
</task>

</tasks>

<verification>
- Gathering tests pass: `evennia test --settings server.conf.settings tests/test_gathering.py`
- Fishing tests pass: `evennia test --settings server.conf.settings tests/test_fishing.py`
- Full suite no regressions: `evennia test --settings server.conf.settings tests/`
</verification>

<success_criteria>
- All 8 success criteria (SC-1 through SC-8) have automated test coverage
- Tests pass with evennia test runner
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/13-gathering-and-refining/13-07-SUMMARY.md`
</output>
