---
phase: 07-milestone-1-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typeclasses/objects.py
  - world/inventory_engine.py
  - tests/test_equipment_slots.py
autonomous: true
requirements:
  - CON-04
must_haves:
  truths:
    - "Equipment with any of the 13 valid slots can be equipped"
    - "Two-handed weapons block off_hand equip and vice versa"
    - "Ring items auto-fill ring1 then ring2"
  artifacts:
    - path: "typeclasses/objects.py"
      provides: "SoravelonEquipment with 13 VALID_SLOTS and two-handed logic"
      contains: "main_hand"
    - path: "world/inventory_engine.py"
      provides: "Updated equip_item with two-handed and ring logic"
      contains: "two_handed"
    - path: "tests/test_equipment_slots.py"
      provides: "Tests for 13-slot equipment system"
  key_links:
    - from: "typeclasses/objects.py"
      to: "world/inventory_engine.py"
      via: "can_equip called by equip_item"
      pattern: "can_equip"
---

<objective>
Expand the equipment slot system from 7 slots to the 13 required by D-37, add two-handed weapon logic (D-38), shield passive armor (D-39), and ring slot auto-fill.

Purpose: Equipment infrastructure must exist before any equipment items are authored in zone specs.
Output: Updated SoravelonEquipment class, updated equip/unequip logic, test suite.
</objective>

<execution_context>
@C:\Users\colek\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\colek\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@typeclasses/objects.py
@world/inventory_engine.py
@world/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand equipment slots and add two-handed weapon logic</name>
  <files>typeclasses/objects.py, world/inventory_engine.py, tests/test_equipment_slots.py</files>
  <read_first>typeclasses/objects.py, world/inventory_engine.py, world/models.py</read_first>
  <behavior>
    - Test: Equipment with slot "chest" can be equipped (new slot name replacing "body")
    - Test: Equipment with slot "face", "back", "wrists", "legs", "ring1", "ring2", "amulet" all valid
    - Test: Equipment with slot "main_hand" valid (replacing "right_hand")
    - Test: Equipment with slot "off_hand" valid (replacing "left_hand")
    - Test: Old slot names ("body", "right_hand", "left_hand", "accessory") are NOT in VALID_SLOTS
    - Test: Two-handed weapon (db.two_handed=True) blocks equip if off_hand is occupied
    - Test: Equipping a two-handed weapon marks both main_hand and off_hand as occupied
    - Test: Cannot equip shield/off_hand item when two-handed weapon is equipped
    - Test: Unequipping two-handed weapon frees both slots
    - Test: Ring equip with ring1 empty goes to ring1
    - Test: Ring equip with ring1 occupied goes to ring2
    - Test: Ring equip with both ring1 and ring2 occupied fails
    - Test: Equipment with db.armor_value stores armor stat (for shields per D-39)
    - Test: Equipment with db.damage_min/db.damage_max stores damage range (per D-42)
    - Test: Equipment with db.stat_bonuses stores stat bonuses dict (per D-40)
  </behavior>
  <action>
    **Per D-37:** Update SoravelonEquipment.VALID_SLOTS to the 13 required slots:
    `{"head", "face", "chest", "back", "hands", "wrists", "legs", "feet", "main_hand", "off_hand", "ring1", "ring2", "amulet"}`

    Remove old slot names: body, right_hand, left_hand, accessory.

    **Per D-38:** Add two-handed weapon support:
    - In `at_object_creation()`, add `self.db.two_handed = False`
    - In `can_equip()`: If item is two_handed and slot is main_hand, also check off_hand is free. If off_hand is occupied, return False with message "You need both hands free for that weapon."
    - If equipping to off_hand, check main_hand item isn't two_handed. If it is, return False with "Your main hand weapon requires both hands."

    **Per D-39:** Add armor_value support:
    - In `at_object_creation()`, add `self.db.armor_value = 0`, `self.db.damage_min = 0`, `self.db.damage_max = 0`
    - Also add `self.db.material_tier = 0` for material tier tracking per D-35

    **Ring auto-fill:** In `can_equip()`, if slot starts with "ring" and the item's equipment_slot is generic "ring", auto-assign to ring1 first, then ring2. Actually simpler: items will have explicit ring1 or ring2 slots. The auto-fill logic goes in equip_item — if slot is "ring1" and ring1 is occupied, try ring2 instead.

    **Update inventory_engine.py:**
    - In `equip_item()`: After equipping a two_handed weapon to main_hand, also mark off_hand as occupied by creating/updating the InventoryItem record with equipment_slot covering both. Use approach: set `record.equipment_slot = "main_hand"` and create a sentinel check — when checking off_hand availability, also check if main_hand item is two_handed.
    - Simpler approach: In can_equip, query if any equipped item in main_hand has two_handed=True when trying to equip off_hand. And query if off_hand is occupied when trying to equip two_handed main_hand.
    - In `unequip_item()`: No change needed — unequipping main_hand two-hander just frees main_hand; off_hand was never physically occupied.

    **Test file:** `tests/test_equipment_slots.py` using unittest.TestCase with MagicMock for character and InventoryItem queries. Test all 13 slots are valid, old slots rejected, two-handed blocking, ring auto-fill.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -m pytest tests/test_equipment_slots.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VALID_SLOTS" typeclasses/objects.py` shows the set with 13 members
    - `grep "main_hand" typeclasses/objects.py` finds the new slot name
    - `grep "two_handed" typeclasses/objects.py` finds two-handed check logic
    - `grep "ring1" typeclasses/objects.py` finds ring slot names
    - `grep "armor_value" typeclasses/objects.py` finds armor attribute init
    - `grep "damage_min" typeclasses/objects.py` finds damage attribute init
    - All tests in test_equipment_slots.py pass
  </acceptance_criteria>
  <done>13 equipment slots work correctly. Two-handed weapons block off_hand. Shields equip to off_hand with armor_value. Ring slots auto-fill. All old slot names removed. Tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_equipment_slots.py -x -v` — all pass
- `grep -c "head.*face.*chest" typeclasses/objects.py` — confirms new slots present
</verification>

<success_criteria>
- SoravelonEquipment.VALID_SLOTS contains exactly 13 slot names per D-37
- Two-handed weapons prevent off_hand equip per D-38
- Shields store armor_value per D-39
- Equipment stores damage_min/damage_max per D-42
- Equipment stores stat_bonuses per D-40
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-1-content/07-01-SUMMARY.md`
</output>
