---
phase: 07-milestone-1-content
plan: 07
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified:
  - world/areas/cantera_edge.py
  - world/mob_templates.py
  - world/loot_tables.py
autonomous: true
requirements:
  - CON-02
  - CON-03
must_haves:
  truths:
    - "Zone has 100+ rooms themed to forest biome"
    - "This is the node zone — area.node() call present with layer_1_overrides per D-45/D-52"
    - "3-5 base mob types themed to forest per D-31"
    - "One named mob with unique loot per D-20"
    - "Cross-zone exits to city and adjacent zones per D-25"
    - "Rooms near node center have layer_1_overrides for corrupted versions per D-47"
  artifacts:
    - path: "world/areas/cantera_edge.py"
      provides: "Forest starter zone with node system, 100+ rooms"
      contains: "def build"
  key_links:
    - from: "world/areas/cantera_edge.py"
      to: "world/zone_object.py"
      via: "area.node() triggers initialize_node"
      pattern: "area.node"
---

<objective>
Author Starter Zone 3: Cantera Edge — a dense, ancient forest zone west of Vael's Crossing with 100+ rooms. This zone is the NODE ZONE (per D-45) with an active node and Layer 1 rooms for the collapsed state.

Purpose: Forest biome with magical instability. This zone demonstrates the node failure mechanic (healthy -> stressed -> failing -> collapsed) and Layer 1 room transformation. Chosen for the node because dense forest naturally fits corruption/magical instability themes.
Output: world/areas/cantera_edge.py with node configuration + Layer 1 overrides + mob templates + loot tables.
</objective>

<execution_context>
@C:\Users\colek\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\colek\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-milestone-1-content/07-02-SUMMARY.md

@world/area_builder.py
@world/area_validator.py
@world/mob_templates.py
@world/loot_tables.py
@world/zone_object.py
@world/scripts/node_script.py
@world/room_state.py
@C:\Obsidian\brain\Soravelon\Soravelon_World_Bible.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cantera Edge zone spec with node system and Layer 1 overrides</name>
  <files>world/areas/cantera_edge.py, world/mob_templates.py, world/loot_tables.py</files>
  <read_first>world/area_builder.py, world/area_validator.py, world/mob_templates.py, world/loot_tables.py, world/zone_object.py, world/scripts/node_script.py, world/room_state.py, C:\Obsidian\brain\Soravelon\Soravelon_World_Bible.md</read_first>
  <action>
    **Per D-17:** 100+ rooms, forest biome, Cantera Forest reference from vault geography.
    **Per D-45:** This is the node zone — Claude's pick based on forest/corruption thematic fit.
    **Per D-18:** NO LEVELS anywhere.

    **Create world/areas/cantera_edge.py:**

    Zone setup:
    ```python
    area.zone("cantera_edge", "The Cantera Edge",
              zone_type="ancient_forest", continent="varath",
              faction_territory=None,  # Contested/wild
              world_x=-60, world_y=0, world_radius=60)
    ```

    **Room design (~100+ rooms):**
    - Dense forest paths, overgrown ruins, moss-covered clearings
    - Central area around the node center (ancient grove or stone circle)
    - Paths branch from edges toward the mysterious center
    - Room types: path, clearing, ruins, cave (root caves)
    - The deeper you go, the more the descriptions hint at magical instability

    **Per D-52:** Choose node type from VALID_NODE_TYPES — "resonance" fits forest (ancient magic humming through roots/stones).

    **Node setup (per D-45/D-46/D-47/D-49):**
    ```python
    area.node(grove_center, radius=5,
              node_type="resonance",
              lore_fragments=["cantera_node_lore_001", "cantera_node_lore_002"],
              layer_1_overrides={...})
    ```

    **Per D-47:** Partial overlay — only rooms within ~radius 5 of node center get Layer 1 versions. That's roughly 15-25 rooms. Edge rooms stay normal.

    **Per D-46:** Layer 1 overrides must be a COMPLETE transformation. Example:
    - Normal: "Sunlit Clearing" → Layer 1: "Twisted Hollow" with completely different description showing corruption
    - Normal: "Ancient Oak Path" → Layer 1: "Petrified Passage" with stone-like trees, wrong-colored light
    - Each L1 room description should feel almost unrecognizable from L0

    **Per D-48:** Unique corrupted mob variants spawn only in Layer 1. Add to mob_templates.py:
    - "corrupted_treant" — was a tree, now animated and hostile
    - "void_wisp" — magical residue entity, erratic behavior
    - "blighted_stag" — corrupted wildlife, aggressive

    These get `spawn_condition="node_active"` in spawn defs on the rooms with L1 overrides.

    **Per D-50:** Layer 1 exclusive lore fragments for Remnance/Echoes domain. Include 3-5 node-exclusive lore fragments in the layer_1_overrides rooms.

    **Per D-31: 3-5 base mob types** (normal/L0, add to mob_templates.py):
    - "forest_spider" — aggressive, web/bite, medium stats
    - "wild_boar" — cautious, charge/gore, high HP
    - "cantera_wolf" — aggressive pack predator, similar to ash_wolf variant
    - "vine_creeper" — passive until stepped on, entangle/constrict
    - "forest_bandit" — cautious humanoid, bow/sword abilities

    **Per D-20:** Named mob — "The Heartwood Ancient" (enormous corrupted treant near node center, or a massive spider in the deep forest). Unique loot, 120-min respawn.

    **Per D-22:** Field NPCs:
    - Druid hermit studying the node instability
    - Warden ranger tracking corruption spread
    - Lost traveler (quest hook about missing supplies)

    **Per D-24:** Materials: timber tier 1, rare herbs, mushrooms. Lore fragments about the forest's ancient history and the node.
    **Per D-25:** Cross-zone exits: east to vaels_crossing, south to plains, north to foothills.
    **Per D-27/D-28:** Wild boar and lone wolves wander. 1 forest_spider as is_hunter.

    **Add loot tables** for all forest mob types + corrupted variants.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -c "from world.areas.cantera_edge import build; print('Zone spec imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "area.room" world/areas/cantera_edge.py` shows 100+ rooms
    - `grep "area.node" world/areas/cantera_edge.py` confirms node configuration
    - `grep "layer_1_overrides" world/areas/cantera_edge.py` confirms L1 room overrides
    - `grep "resonance" world/areas/cantera_edge.py` confirms node_type choice
    - `grep "corrupted\|void_wisp\|blighted" world/mob_templates.py` confirms corrupted mob templates
    - `grep "node_active" world/areas/cantera_edge.py` confirms L1 spawn conditions
    - `grep "named_mob" world/areas/cantera_edge.py` confirms mini-boss
    - `grep -c "area.npc" world/areas/cantera_edge.py` shows 1-3 NPCs
    - `python -c "from world.areas.cantera_edge import build"` succeeds
  </acceptance_criteria>
  <done>Cantera Edge has 100+ rooms, resonance node with Layer 1 overrides for ~20 rooms near center, corrupted mob variants for L1, 5 normal mob types, named mob, field NPCs, cross-zone exits. Zone spec imports without error.</done>
</task>

</tasks>

<verification>
- Zone spec imports without error
- Node configuration present with layer_1_overrides
- Room count >= 100
</verification>

<success_criteria>
- Cantera Edge is a complete starter zone per CON-02
- Node system properly configured per CON-03
- Layer 1 rooms are complete transformations per D-46
- Corrupted mob variants exist for Layer 1 per D-48
</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-1-content/07-07-SUMMARY.md`
</output>
