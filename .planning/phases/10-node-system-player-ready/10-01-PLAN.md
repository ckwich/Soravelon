---
phase: 10-node-system-player-ready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - world/zone_object.py
  - world/scripts/node_script.py
autonomous: true
requirements:
  - NODE-L1-EXITS
  - NODE-L1-OVERRIDES
must_haves:
  truths:
    - "L1 rooms have exits mirroring L0 topology so players can navigate the shadow dimension"
    - "L1 rooms display override names and descriptions when the node is active"
    - "L1 rooms revert to default names when the node deactivates"
  artifacts:
    - path: "world/zone_object.py"
      provides: "Exit cloning in initialize_node()"
      contains: "create_object"
    - path: "world/scripts/node_script.py"
      provides: "Override application in _activate_layer1() and restoration in _deactivate_layer1()"
      contains: "layer_1_overrides"
  key_links:
    - from: "world/zone_object.py"
      to: "typeclasses.exits.SoravelonExit"
      via: "create_object for L1 exits"
      pattern: "SoravelonExit"
    - from: "world/scripts/node_script.py"
      to: "zone_obj.db.layer_1_overrides"
      via: "dict lookup by room tag or id"
      pattern: "layer_1_overrides"
---

<objective>
Create navigable Layer 1 rooms by cloning L0 exit topology into L1 at node initialization, and apply/restore override descriptions during node activation/deactivation.

Purpose: Without exits, L1 rooms are isolated boxes players cannot navigate. Without overrides, L1 rooms lack atmospheric identity. Both are required for the node system to deliver its intended player experience.

Output: Modified world/zone_object.py (exit cloning) and world/scripts/node_script.py (override application/restoration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-node-system-player-ready/10-CONTEXT.md

@world/zone_object.py
@world/scripts/node_script.py
@typeclasses/rooms.py
@world/nodes/node_effects.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From world/zone_object.py:
```python
def initialize_node(zone_obj, node_type, center_room, radius,
                    layer0_rooms, failure_start=0):
    # Creates L1 rooms, stores layer0_room.db.layer1_room_id on each L0 room
    # Returns NodeScript
```

From world/scripts/node_script.py:
```python
class NodeScript(DefaultScript):
    def _activate_layer1(self):
        # Teleports players from L0 to L1 rooms
        # Currently does NOT apply overrides
    def _deactivate_layer1(self):
        # Teleports players back from L1 to L0
        # Currently does NOT restore names
```

From typeclasses/exits.py:
```python
# SoravelonExit is the base exit typeclass
```

L0-L1 mapping: Each L0 room has `db.layer1_room_id` pointing to its L1 counterpart.
zone_obj.db.layer_1_overrides: dict keyed by room identifier (string), value is {name, desc}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone L0 exits into L1 rooms in initialize_node()</name>
  <files>world/zone_object.py</files>
  <read_first>world/zone_object.py, typeclasses/rooms.py, typeclasses/exits.py</read_first>
  <action>
In `initialize_node()`, after all L1 rooms are created (after the existing `for layer0_room in layer0_rooms` loop), add a second pass that clones exits. Per D-02, exits are created at build time, not dynamically at activation.

Build a mapping dict `l0_to_l1 = {}` during the first loop: `l0_to_l1[layer0_room.id] = layer1_room.id`.

Then iterate all `layer0_rooms` again. For each L0 room, iterate `layer0_room.exits`. For each exit where `exit_obj.destination` is in `l0_to_l1` (both source and destination have L1 counterparts per D-03), create a SoravelonExit:

```python
from typeclasses.exits import SoravelonExit
l1_exit = create_object(
    SoravelonExit,
    key=exit_obj.key,
    location=l1_source,   # L1 room for this L0 room
    destination=l1_dest,   # L1 room for exit's L0 destination
)
l1_exit.tags.add("inactive", category="node_layer")
```

Tag each L1 exit with `inactive` in `node_layer` category (mirrors L1 room tagging pattern). The exit exists permanently but is tagged inactive until the node activates.

Also add the zone_id tag to L1 exits: `l1_exit.tags.add(zone_obj.db.zone_id, category="zone_id")` if zone_id exists.

Do NOT modify any other function. Only add code after the existing L1 room creation loop.
  </action>
  <verify>
    <automated>cd C:\Dev\Evennia\soravelon && python -c "
from world.zone_object import initialize_node
import inspect
src = inspect.getsource(initialize_node)
assert 'SoravelonExit' in src, 'Missing SoravelonExit import/usage'
assert 'l0_to_l1' in src or 'layer0_to_layer1' in src or 'exit_obj.destination' in src, 'Missing exit cloning logic'
assert 'node_layer' in src, 'Missing node_layer tag on exits'
print('PASS: initialize_node has exit cloning')
"</automated>
  </verify>
  <acceptance_criteria>
- initialize_node() creates SoravelonExit objects linking L1 rooms that mirror L0 exit topology
- L1 exits are tagged inactive in node_layer category
- Only exits where BOTH source and destination have L1 counterparts are cloned (per D-03)
  </acceptance_criteria>
  <done>L1 rooms have mirrored exits created at build time, tagged inactive until node activates</done>
</task>

<task type="auto">
  <name>Task 2: Apply and restore L1 overrides during activation/deactivation</name>
  <files>world/scripts/node_script.py</files>
  <read_first>world/scripts/node_script.py, world/zone_object.py, world/nodes/node_effects.py</read_first>
  <action>
Modify `_activate_layer1()` in NodeScript to apply `layer_1_overrides` from `self.obj.db.layer_1_overrides` (the zone_obj). Per D-04 and D-05:

After the existing line `layer1_room.tags.add("active", category="node_layer")`, add override application:

```python
overrides = (self.obj.db.layer_1_overrides or {}) if self.obj else {}
```

For each L1 room found in the loop, look up its corresponding L0 room's identifier in the overrides dict. The override dict is keyed by room_id string (the area builder's room identifier). The L0 room can be found via `layer0_room` (already in scope). Check `layer0_room.db.room_id` or `layer0_room.key` as lookup key.

If an override exists for this room:
- Store original name: `layer1_room.db.original_name = layer1_room.key`
- Apply: `layer1_room.key = override.get("name", layer1_room.key)`
- Apply desc: `layer1_room.db.desc = override.get("desc", "")`

If NO override exists for this room (per D-05):
- Prefix name: `layer1_room.key = f"[Distorted] {layer0_room.key}"`
- Store original: `layer1_room.db.original_name` set before change

Also activate L1 exits in this room: find exits in `layer1_room.exits` (or search by zone), swap their `node_layer` tag from inactive to active.

Modify `_deactivate_layer1()` to restore original names:

For each L1 room in the deactivation loop, after teleporting players back:
- Restore: `layer1_room.key = layer1_room.db.original_name or layer1_room.key`
- Clear desc override: (optional, desc stays since room is inactive anyway)
- Deactivate L1 exits: swap `node_layer` tag from active to inactive on each exit in the room.

Also apply node_state tags to L1 rooms (discretion: yes, for consistency) -- in `_apply_state_tags` and `_remove_state_tags`, also iterate L1 rooms from `self.db.layer1_room_ids`.
  </action>
  <verify>
    <automated>cd C:\Dev\Evennia\soravelon && python -c "
import inspect
from world.scripts.node_script import NodeScript
src_activate = inspect.getsource(NodeScript._activate_layer1)
src_deactivate = inspect.getsource(NodeScript._deactivate_layer1)
assert 'layer_1_overrides' in src_activate, 'Missing override application in _activate_layer1'
assert 'original_name' in src_activate, 'Missing original name storage'
assert 'original_name' in src_deactivate, 'Missing name restoration in _deactivate_layer1'
assert 'Distorted' in src_activate, 'Missing default prefix for rooms without overrides'
print('PASS: Override application and restoration present')
"</automated>
  </verify>
  <acceptance_criteria>
- _activate_layer1() reads layer_1_overrides from zone_obj and applies name+desc to L1 rooms
- Rooms without overrides get "[Distorted] original_name" prefix per D-05
- _deactivate_layer1() restores original names from db.original_name
- L1 exits toggle between inactive/active node_layer tags during activation/deactivation
  </acceptance_criteria>
  <done>L1 rooms show override descriptions when active, revert when deactivated; L1 exits are navigable only during active state</done>
</task>

</tasks>

<verification>
- `python -c "import world.zone_object; import world.scripts.node_script; print('imports OK')"` succeeds
- initialize_node creates L1 exits with correct topology
- _activate_layer1 applies overrides and activates exits
- _deactivate_layer1 restores names and deactivates exits
</verification>

<success_criteria>
- L1 rooms created by initialize_node have exits mirroring L0 topology
- Override names/descriptions applied during activation, restored during deactivation
- Rooms without overrides show "[Distorted]" prefix
- L1 exits tagged inactive at creation, toggled to active during node activation
</success_criteria>

<output>
After completion, create `.planning/phases/10-node-system-player-ready/10-01-SUMMARY.md`
</output>
