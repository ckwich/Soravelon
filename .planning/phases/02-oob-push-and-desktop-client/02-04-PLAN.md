---
phase: 02-oob-push-and-desktop-client
plan: 04
type: tdd
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - tests/test_oob_publisher.py
  - tests/test_area_builder.py
autonomous: true
requirements:
  - CLI-06
  - CLI-07

must_haves:
  truths:
    - "All 8 push_* functions have passing unit tests using MagicMock character"
    - "Debounce prevents duplicate sends within the configured window"
    - "Debounce allows sends after the window expires"
    - "No OOB send occurs when character has no connected sessions"
    - "Auto-layout assigns non-overlapping coords to all rooms after build()"
    - "Builder-placed grid_x/grid_y are preserved by auto-layout"
    - "map_update payload contains x and y for every room in the zone"
  artifacts:
    - path: "tests/test_oob_publisher.py"
      provides: "Full test suite for CLI-06: all 8 message types, debounce, session guard"
    - path: "tests/test_area_builder.py"
      provides: "Coordinate tests for CLI-07: auto-layout coverage, explicit coord preservation"
  key_links:
    - from: "tests/test_oob_publisher.py"
      to: "world/oob_publisher.py"
      via: "MagicMock character, assert_called_with / assert_called_once"
      pattern: "from world.oob_publisher import"
    - from: "tests/test_area_builder.py"
      to: "world/area_builder.py"
      via: "EvenniaTest build() then check room.db.grid_x"
      pattern: "grid_x\|grid_y\|auto_layout"
---

<objective>
Write the complete test suite for Phase 2. CLI-06 tests go in a new file (test_oob_publisher.py)
using unittest.TestCase + MagicMock per the project pattern established in test_flight_engine.py.
CLI-07 coordinate tests extend the existing test_area_builder.py.

Purpose: Automated verification that the OOB publisher's contract, debounce logic, and
coordinate system work correctly before Phase 2.1 (Tauri client) consumes them.

Output: tests/test_oob_publisher.py (new), tests/test_area_builder.py (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-oob-push-and-desktop-client/02-CONTEXT.md
@.planning/phases/02-oob-push-and-desktop-client/02-RESEARCH.md
@.planning/phases/02-oob-push-and-desktop-client/02-01-SUMMARY.md
@.planning/phases/02-oob-push-and-desktop-client/02-02-SUMMARY.md

<interfaces>
<!-- Test patterns the executor must follow. -->

MagicMock character setup (from RESEARCH.md Code Examples + project convention in test_flight_engine.py):
```python
import unittest
from unittest.mock import MagicMock, patch
import sys, types

class TestOobPublisher(unittest.TestCase):
    def setUp(self):
        self.char = MagicMock()
        self.char.ndb.oob_debounce = None          # start as None — Pitfall 1
        self.char.sessions.all.return_value = [MagicMock()]  # simulates connected session
        # DB attrs needed by push_status_update:
        self.char.db.reputation_score = 42.0
        self.char.db.network_score = 10.0
        self.char.db.bond_score = 5.0
        self.char.db.legacy_score = 0.0
        self.char.db.attunement_score = 20.0
        self.char.db.domain_scores = {"combat": 35}
        self.char.db.carried_scales = 100
        self.char.db.companion_id = None
        self.char.db.companion_type = None
        self.char.db.companion_tier = None
        # For map_update, char needs location:
        self.char.location = MagicMock()
        self.char.location.db.zone_id = "test_zone"
        self.char.location.db.fog_of_war = False
        self.char.location.tags.get.return_value = "test_room_001"
        self.char.db.visited_room_ids = set()
```

sys.modules injection for world.world_state (avoids Django setup in unittest.TestCase):
The test file should inject stubs for world.world_state and world.group_engine before
importing oob_publisher — same pattern as test_flight_engine.py.

EvenniaTest pattern for test_area_builder.py (file uses EvenniaTest already):
The existing AreaBuilderTestBase class provides _make_builder() and _make_room().
New test classes extend AreaBuilderTestBase. Call ab.exit() then ab.build() to trigger
auto_layout_zone. Check room.db.grid_x after build().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_oob_publisher.py — CLI-06 coverage</name>
  <files>tests/test_oob_publisher.py</files>
  <read_first>
    - tests/test_flight_engine.py (sys.modules injection pattern — follow exactly)
    - world/oob_publisher.py (all push_* signatures, DEBOUNCE_INTERVALS values)
    - .planning/phases/02-oob-push-and-desktop-client/02-RESEARCH.md (Test pattern section, Pitfall 1 ndb None, Pitfall 2 no sessions)
  </read_first>
  <action>
    Create tests/test_oob_publisher.py as a NEW file.

    Module structure:
    1. sys.modules injection block (before any world.* imports)
    2. Test classes — one class per behavior scenario

    SYS.MODULES INJECTION (copy the exact pattern from test_flight_engine.py — replace
    the flight-specific stubs with the stubs needed here):
    - Stub world.world_state: needs get_character_context_packet returning a dict with
      all dimension score keys (reputation_score, network_score, bond_score, legacy_score,
      attunement_score), domain_scores, companion info
    - Stub world.group_engine: needs a get_group_state stub returning None
    - Stub world.inventory_helpers: needs get_carry_state returning "light"
    - Stub world.inventory_engine: needs get_inventory (if used) returning []
    - Stub evennia: needs search_tag returning []

    After the stubs, import: from world.oob_publisher import (all 8 push functions,
    DEBOUNCE_INTERVALS, _should_send, _mark_sent)

    TEST CLASSES — write ONE class per logical scenario (project convention):

    class TestPushStatusUpdate(unittest.TestCase):
      - test_sends_status_update_kwarg: char.msg called with status_update kwarg
      - test_dimensions_included: status_update["dimensions"]["reputation"] == 42.0
      - test_domains_included: status_update["domains"] == {"combat": 35}
      - test_no_send_when_no_session: char.sessions.all returns [] → char.msg NOT called

    class TestPushStatUpdate(unittest.TestCase):
      - test_sends_stat_update: char.msg called with stat_update kwarg
      - test_placeholder_fields: stat_update["hp"] is None, stat_update["conditions"] == []

    class TestPushNodeEvent(unittest.TestCase):
      - test_sends_node_event: push_node_event(char, "z1", "dormant", "awakening")
        → char.msg called with node_event={"zone_id":"z1","old_state":"dormant","new_state":"awakening"}
      - test_no_debounce: two rapid push_node_event calls both send (interval is 0.0)

    class TestPushFlightProgress(unittest.TestCase):
      - test_sends_flight_progress: push_flight_progress(char, 0, 3, "Varath Landing", False)
        → char.msg called with flight_progress kwarg containing leg_index=0, total_legs=3

    class TestDebounceGate(unittest.TestCase):
      setUp: char with sessions connected, ndb.oob_debounce = None
      - test_first_call_sends: push_status_update(char) → char.msg called once
      - test_second_call_within_window_blocked: two push_status_update calls in immediate
        succession (no time advance) → char.msg called only ONCE (second blocked)
      - test_call_after_window_allowed: manually set ndb.oob_debounce =
        {"status_update": time.monotonic() - 3.0} (past the 2.0s window) → push_status_update
        sends (char.msg called)
      - test_node_event_never_debounced: two push_node_event calls → char.msg called TWICE

    class TestSessionGuard(unittest.TestCase):
      - test_no_send_disconnected: char.sessions.all returns [] → char.msg.assert_not_called()
      - test_sends_when_connected: char.sessions.all returns [MagicMock()] → char.msg called

    class TestShouldSendHelper(unittest.TestCase):
      - test_returns_true_when_debounce_none: _should_send(char, "status_update") is True
        (first call, ndb is None)
      - test_returns_false_within_window: set ndb.oob_debounce =
        {"status_update": time.monotonic()} → _should_send returns False
      - test_returns_true_for_zero_interval: _should_send(char, "node_event") is True always

    Use `import time` in the test class that needs time.monotonic() for window manipulation.
    Each test is self-contained — setUp creates fresh MagicMock.
  </action>
  <verify>
    <automated>evennia test --settings server.conf.settings tests/test_oob_publisher.py --verbosity=2 2>&1 | tail -20</automated>
  </verify>
  <done>
    tests/test_oob_publisher.py exists. All test classes pass. Command output shows
    "OK" with 0 failures. At minimum 15 test methods covering all 8 message types,
    debounce logic (within-window block, after-window allow, zero-interval pass),
    and session guard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add coordinate tests to tests/test_area_builder.py — CLI-07 coverage</name>
  <files>tests/test_area_builder.py</files>
  <read_first>
    - tests/test_area_builder.py (full file — understand AreaBuilderTestBase, _make_builder, _make_room patterns)
    - world/area_builder.py (auto_layout_zone function, DIRECTION_OFFSETS, _set_room_attrs changes)
    - .planning/phases/02-oob-push-and-desktop-client/02-RESEARCH.md (Pitfall 4 collision on up/down)
  </read_first>
  <action>
    APPEND new test classes to the END of tests/test_area_builder.py. Do NOT modify any
    existing test classes or the AreaBuilderTestBase class.

    The new test classes all extend AreaBuilderTestBase and call ab.build() so auto-layout runs.

    CLASS 1: TestAutoLayoutAssignsCoords(AreaBuilderTestBase)
      def test_linear_north_chain_assigns_distinct_coords(self):
        """3-room north chain → rooms get (0,0), (0,1), (0,2)."""
        ab = self._make_builder("coord_zone_1")
        r1 = self._make_room(ab, "r1")
        r2 = self._make_room(ab, "r2")
        r3 = self._make_room(ab, "r3")
        ab.exit(r1, r2, "north")
        ab.exit(r2, r3, "north")
        ab.build()
        self.assertEqual(r1.db.grid_x, 0)
        self.assertEqual(r1.db.grid_y, 0)
        self.assertEqual(r2.db.grid_x, 0)
        self.assertEqual(r2.db.grid_y, 1)
        self.assertEqual(r3.db.grid_x, 0)
        self.assertEqual(r3.db.grid_y, 2)

      def test_all_rooms_get_coords_after_build(self):
        """Every room in the zone has non-None grid_x and grid_y after build()."""
        ab = self._make_builder("coord_zone_2")
        rooms = [self._make_room(ab, f"rx{i}") for i in range(5)]
        for i in range(len(rooms) - 1):
            ab.exit(rooms[i], rooms[i+1], "east")
        ab.build()
        for r in rooms:
            self.assertIsNotNone(r.db.grid_x, f"room {r.key} has no grid_x")
            self.assertIsNotNone(r.db.grid_y, f"room {r.key} has no grid_y")

      def test_no_two_rooms_share_coordinates(self):
        """Auto-layout collision resolution: no two rooms share (x, y) even with up/down exits."""
        ab = self._make_builder("coord_zone_3")
        r1 = self._make_room(ab, "base")
        r2 = self._make_room(ab, "above")
        r3 = self._make_room(ab, "below")
        ab.exit(r1, r2, "up")
        ab.exit(r1, r3, "down")
        ab.build()
        coords = [(r.db.grid_x, r.db.grid_y) for r in (r1, r2, r3)]
        self.assertEqual(len(coords), len(set(coords)), "Two rooms share the same coordinates")

    CLASS 2: TestExplicitCoordsPreserved(AreaBuilderTestBase)
      def test_explicit_coords_not_overwritten(self):
        """Rooms with grid_x/grid_y in area spec keep those exact values after build()."""
        ab = self._make_builder("coord_zone_4")
        r1 = self._make_room(ab, "placed_1", grid_x=5, grid_y=3)
        r2 = self._make_room(ab, "placed_2", grid_x=6, grid_y=3)
        ab.exit(r1, r2, "east")
        ab.build()
        self.assertEqual(r1.db.grid_x, 5)
        self.assertEqual(r1.db.grid_y, 3)
        self.assertEqual(r2.db.grid_x, 6)
        self.assertEqual(r2.db.grid_y, 3)

      def test_mixed_explicit_and_auto(self):
        """One room with explicit coords, adjacent room gets auto-layout."""
        ab = self._make_builder("coord_zone_5")
        r1 = self._make_room(ab, "anchor", grid_x=10, grid_y=0)
        r2 = self._make_room(ab, "auto_placed")  # no explicit coords
        ab.exit(r1, r2, "east")
        ab.build()
        self.assertEqual(r1.db.grid_x, 10)   # unchanged
        self.assertIsNotNone(r2.db.grid_x)   # auto-layout filled it in

    CLASS 3: TestZoneWorldCoords(AreaBuilderTestBase)
      def test_world_coords_stored_on_zone_obj(self):
        ab = self._make_builder("world_coord_zone")
        # Re-call zone() with world coord kwargs — use a fresh builder with coords
        ab2 = AreaBuilder("world_coord_zone_2")
        ab2.zone(
            name="World Coord Zone",
            zone_type="plains",
            continent="varath",
            world_x=15,
            world_y=8,
            world_radius=3,
            fog_of_war=True,
        )
        self.assertEqual(ab2._zone_obj.db.world_x, 15)
        self.assertEqual(ab2._zone_obj.db.world_y, 8)
        self.assertEqual(ab2._zone_obj.db.world_radius, 3)
        self.assertTrue(ab2._zone_obj.db.fog_of_war)

      def test_fog_of_war_defaults_false(self):
        ab = self._make_builder("fog_default_zone")
        self.assertFalse(ab._zone_obj.db.fog_of_war)

    Import AreaBuilder at the top of the new test classes — it is already imported
    at the top of the existing test file, so no new import is needed.
  </action>
  <verify>
    <automated>evennia test --settings server.conf.settings tests/test_area_builder.py --verbosity=2 2>&1 | tail -20</automated>
  </verify>
  <done>
    All new TestAutoLayoutAssignsCoords, TestExplicitCoordsPreserved, TestZoneWorldCoords
    test methods pass. All previously-passing test_area_builder tests still pass.
    No failures, no errors.
  </done>
</task>

</tasks>

<verification>
Full suite green:
- evennia test --settings server.conf.settings tests/test_oob_publisher.py → OK
- evennia test --settings server.conf.settings tests/test_area_builder.py → OK
- evennia test --settings server.conf.settings tests/ → no regressions in any file

Coverage check:
- grep -c "def test_" tests/test_oob_publisher.py returns >= 15
- grep -c "def test_" tests/test_area_builder.py increased by >= 8 from baseline
</verification>

<success_criteria>
- tests/test_oob_publisher.py exists with >= 15 passing tests
- All 8 message types have at least one test verifying the OOB kwarg name and at least one key field
- Debounce within-window block and after-window allow both have passing tests
- Session guard (no send when disconnected) has a passing test
- test_area_builder.py has >= 8 new coordinate tests all passing
- Full test suite shows 0 failures after this plan
</success_criteria>

<output>
After completion, create .planning/phases/02-oob-push-and-desktop-client/02-04-SUMMARY.md
</output>
