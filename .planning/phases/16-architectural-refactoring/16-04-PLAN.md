---
phase: 16-architectural-refactoring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - world/models.py
  - world/mob_spawner.py
  - world/mob_templates.py
  - world/quest_engine.py
  - world/combat_ai.py
  - world/combat_script.py
  - world/zone_serializer.py
  - world/area_builder.py
  - world/action_vocabulary.py
  - typeclasses/mobs.py
  - tests/test_mob_spawner.py
  - tests/test_quest_engine.py
  - tests/test_world_state.py
  - tests/test_spawn_record.py
  - world/migrations/0008_rename_mob_template_key.py
autonomous: true
requirements: [D-11, D-12]

must_haves:
  truths:
    - "SpawnRecord.mob_template is renamed to SpawnRecord.mob_template_key with Django RenameField migration"
    - "mob.db.mob_template is renamed to mob.db.mob_template_key in mob_templates.py and all readers"
    - "Tag category 'mob_id' is renamed to 'mob_instance_id' via constant MOB_INSTANCE_TAG_CATEGORY"
    - "All references updated atomically — zero old-name occurrences remain in production code"
  artifacts:
    - path: "world/migrations/0008_rename_mob_template_key.py"
      provides: "Django RenameField migration for SpawnRecord.mob_template -> mob_template_key"
      contains: "RenameField"
    - path: "world/mob_spawner.py"
      provides: "MOB_INSTANCE_TAG_CATEGORY constant"
      contains: "MOB_INSTANCE_TAG_CATEGORY"
  key_links:
    - from: "world/mob_templates.py"
      to: "mob.db.mob_template_key"
      via: "apply_template sets the db attribute"
      pattern: "mob_template_key"
    - from: "world/mob_spawner.py"
      to: "Evennia tag table"
      via: "tags.add with MOB_INSTANCE_TAG_CATEGORY"
      pattern: "MOB_INSTANCE_TAG_CATEGORY"
---

<objective>
Rename mob_template -> mob_template_key and mob_id tag -> mob_instance_id across the entire codebase per D-11/D-12.

Purpose: Make the distinction explicit between template registry keys and spawned instance tags. This is a breaking rename that must be done atomically — every reference updated in one commit.
Output: All files updated, Django migration for SpawnRecord field, tag category constant defined.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-architectural-refactoring/16-RESEARCH.md
@world/models.py
@world/mob_spawner.py
@world/mob_templates.py
@world/quest_engine.py
@typeclasses/mobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename mob_template -> mob_template_key (Django model + all code references)</name>
  <files>world/models.py, world/migrations/0008_rename_mob_template_key.py, world/mob_templates.py, world/mob_spawner.py, world/quest_engine.py, world/action_vocabulary.py, tests/test_mob_spawner.py, tests/test_quest_engine.py, tests/test_spawn_record.py</files>
  <read_first>world/models.py, world/mob_templates.py, world/mob_spawner.py, world/quest_engine.py, world/action_vocabulary.py, tests/test_mob_spawner.py, tests/test_quest_engine.py, tests/test_spawn_record.py</read_first>
  <action>
Per D-11/D-12: Rename `mob_template` to `mob_template_key` everywhere.

**Step 1: Django model rename (world/models.py)**
Change SpawnRecord field:
```python
mob_template_key = models.CharField(max_length=64)  # was mob_template
```
Also update `__str__` to use `self.mob_template_key`.

**Step 2: Create Django RenameField migration**
Create `world/migrations/0008_rename_mob_template_key.py`:
```python
from django.db import migrations

class Migration(migrations.Migration):
    dependencies = [
        ('world', '0007_merge_20260326_2012'),
    ]
    operations = [
        migrations.RenameField(
            model_name='spawnrecord',
            old_name='mob_template',
            new_name='mob_template_key',
        ),
    ]
```
CRITICAL: Use RenameField, NOT RemoveField+AddField. RenameField preserves data.

**Step 3: Update all code references**
Search and replace in each file. Key locations:

- `world/mob_templates.py` line ~1767: `mob.db.mob_template = template_key` -> `mob.db.mob_template_key = template_key`
- `world/mob_spawner.py` line ~126: `getattr(obj.db, "mob_template", None)` -> `getattr(obj.db, "mob_template_key", None)`
- `world/mob_spawner.py` line ~469: `"mob_template": spawn_def.get("mob", "")` -> `"mob_template_key": spawn_def.get("mob", "")`
- `world/quest_engine.py` line ~220: `mob.db.mob_template` -> `mob.db.mob_template_key`
- `world/action_vocabulary.py` lines ~199-212: variable name `mob_template` -> `mob_template_key` (local variable rename)

Update ALL test files that reference mob_template:
- tests/test_mob_spawner.py
- tests/test_quest_engine.py
- tests/test_spawn_record.py

After all changes, verify zero remaining occurrences:
`grep -rn "mob_template\b" world/*.py typeclasses/*.py commands/*.py tests/*.py | grep -v mob_template_key | grep -v migrations/ | grep -v __pycache__`
Should return 0 hits (excluding migration files which keep old name in RenameField).
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && grep -rn "\.mob_template\b" world/*.py typeclasses/*.py tests/*.py 2>/dev/null | grep -v mob_template_key | grep -v migrations/ | grep -v __pycache__ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - SpawnRecord model field is named `mob_template_key` (not `mob_template`)
    - Migration 0008 exists with RenameField operation
    - `grep -rn "db\.mob_template\b" world/*.py typeclasses/*.py` returns 0 hits (all renamed to mob_template_key)
    - mob_templates.py sets `mob.db.mob_template_key`
    - quest_engine.py reads `mob.db.mob_template_key`
    - mob_spawner.py uses `mob_template_key` in SpawnRecord creation
  </acceptance_criteria>
  <done>mob_template renamed to mob_template_key across all production code, model, and tests</done>
</task>

<task type="auto">
  <name>Task 2: Rename mob_id tag category -> mob_instance_id with constant</name>
  <files>world/mob_spawner.py, typeclasses/mobs.py, world/quest_engine.py, world/combat_ai.py, world/combat_script.py, world/zone_serializer.py, world/area_builder.py, tests/test_mob_spawner.py, tests/test_quest_engine.py, tests/test_world_state.py</files>
  <read_first>world/mob_spawner.py, typeclasses/mobs.py, world/quest_engine.py, world/combat_ai.py, world/combat_script.py, world/zone_serializer.py, world/area_builder.py</read_first>
  <action>
Per D-11/D-12: Rename the Evennia tag category `"mob_id"` to `"mob_instance_id"`.

**Step 1: Define constant in mob_spawner.py**
At module top level:
```python
MOB_INSTANCE_TAG_CATEGORY = "mob_instance_id"
```

**Step 2: Update mob_spawner.py**
- Line ~120: `evennia.search_tag(mob_key, category="mob_id")` -> `evennia.search_tag(mob_key, category=MOB_INSTANCE_TAG_CATEGORY)`
- Line ~201: `mob.tags.add(spawn_def["mob"], category="mob_id")` -> `mob.tags.add(spawn_def["mob"], category=MOB_INSTANCE_TAG_CATEGORY)`

**Step 3: Update typeclasses/mobs.py**
- Line ~189: `self.tags.get("mob_id", category="mob_id")` -> This checks if mob has a tag in the mob_id category. Update to:
  ```python
  from world.mob_spawner import MOB_INSTANCE_TAG_CATEGORY
  is_named = self.tags.get(category=MOB_INSTANCE_TAG_CATEGORY) is not None
  ```
  NOTE: This import can be at top-level since mob_spawner doesn't import from typeclasses (no circular risk). OR use lazy import to be safe.

**Step 4: Update all other files**
- world/quest_engine.py: `mob.db.mob_id` references. NOTE: quest_engine line 221 reads `mob.db.mob_id` which is a db attribute (NOT a tag category). Rename this db attribute to `mob.db.mob_instance_id`.
- world/combat_ai.py: Lines ~521-567 use `mob_id` as a LOCAL VARIABLE holding the integer DB primary key (`mob.id`). This is a DIFFERENT concept. Per research, rename to `combatant_db_id` to avoid confusion, OR leave as-is since it's a local variable with different semantics. Recommended: rename local var to `combatant_db_id` for clarity.
- world/combat_script.py: Line ~57 `pending_mob_casts = {}  # {mob_id: ...}` uses mob_id as dict key holding integer DB pk. Rename comment and key references to `combatant_db_id`.
- world/zone_serializer.py: Line ~119 `mob_id = nm_def["mob_id"]` — this is reading from zone JSON schema. Rename the JSON key to `mob_instance_id` and update the local var.
- world/area_builder.py: `named_mob(self, mob_id, room, **kwargs)` parameter name. Rename to `mob_instance_id`.

**Step 5: Update test files**
- tests/test_mob_spawner.py: update tag category assertions
- tests/test_quest_engine.py: update mob.db.mob_id references
- tests/test_world_state.py: update any mob_id references in test data

**Step 6: Note on existing database tags**
For a development database, existing tags with category="mob_id" will become orphaned. Since mobs are recreated on zone reload (mob_spawner destroys and recreates), this is self-healing — no data migration needed for the tag table. Document this in the summary.

After all changes, verify zero remaining occurrences:
`grep -rn '"mob_id"' world/*.py typeclasses/*.py tests/*.py | grep -v __pycache__ | grep -v migrations/`
Should return 0 hits for the string literal "mob_id".

Run full test suite.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && evennia test --settings server.conf.settings tests/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MOB_INSTANCE_TAG_CATEGORY" world/mob_spawner.py` returns at least 3 (definition + 2 uses)
    - `grep -rn 'category="mob_id"' world/*.py typeclasses/*.py tests/*.py` returns 0 hits
    - `grep -rn 'db\.mob_id\b' world/*.py typeclasses/*.py tests/*.py` returns 0 hits (renamed to mob_instance_id)
    - combat_ai.py local variables renamed to combatant_db_id (or documented why kept)
    - zone_serializer.py uses mob_instance_id key
    - area_builder.py named_mob parameter renamed to mob_instance_id
    - Full test suite passes
  </acceptance_criteria>
  <done>mob_id tag category renamed to mob_instance_id. All code references updated atomically. Tests pass.</done>
</task>

</tasks>

<verification>
- `grep -rn "mob_template\b" world/*.py typeclasses/*.py tests/*.py | grep -v mob_template_key | grep -v migrations/ | grep -v __pycache__` returns 0 hits
- `grep -rn '"mob_id"' world/*.py typeclasses/*.py tests/*.py | grep -v __pycache__` returns 0 hits
- `grep -rn "\.db\.mob_id\b" world/*.py typeclasses/*.py tests/*.py` returns 0 hits
- Migration 0008 exists with RenameField
- Full test suite: `evennia test --settings server.conf.settings tests/`
</verification>

<success_criteria>
- mob_template renamed to mob_template_key in Django model, db attributes, and all code
- mob_id tag category renamed to mob_instance_id with a named constant
- combat_ai.py local variable clarified as combatant_db_id
- Django RenameField migration created
- Zero old-name occurrences in production code
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/16-architectural-refactoring/16-04-SUMMARY.md`
</output>
