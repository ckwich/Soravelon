---
phase: 16-architectural-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - world/session_lifecycle.py
  - world/movement_lifecycle.py
  - world/death_lifecycle.py
  - typeclasses/characters.py
  - typeclasses/mobs.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "Character.at_post_puppet delegates to session_lifecycle.on_login"
    - "Character.at_pre_unpuppet delegates to session_lifecycle.on_logout"
    - "Character.at_after_move delegates to movement_lifecycle.on_move"
    - "SoravelonMob.at_death delegates to death_lifecycle.on_mob_death"
    - "All orchestrators use lazy imports inside functions"
  artifacts:
    - path: "world/session_lifecycle.py"
      provides: "Login/logout orchestration"
      exports: ["on_login", "on_logout"]
    - path: "world/movement_lifecycle.py"
      provides: "Movement orchestration"
      exports: ["on_move"]
    - path: "world/death_lifecycle.py"
      provides: "Mob death orchestration"
      exports: ["on_mob_death"]
  key_links:
    - from: "typeclasses/characters.py"
      to: "world/session_lifecycle.py"
      via: "at_post_puppet calls on_login, at_pre_unpuppet calls on_logout"
      pattern: "from world\\.session_lifecycle import on_login"
    - from: "typeclasses/characters.py"
      to: "world/movement_lifecycle.py"
      via: "at_after_move calls on_move"
      pattern: "from world\\.movement_lifecycle import on_move"
    - from: "typeclasses/mobs.py"
      to: "world/death_lifecycle.py"
      via: "at_death calls on_mob_death"
      pattern: "from world\\.death_lifecycle import on_mob_death"
---

<objective>
Extract typeclass god methods into domain-split lifecycle orchestrators.

Purpose: Move all game logic out of typeclass hooks into world/ orchestrator modules, making typeclasses thin delegates per D-01/D-02/D-03. This enforces the project convention that all game logic lives in world/ modules.
Output: 3 new orchestrator files + 2 modified typeclasses with thin hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@typeclasses/characters.py
@typeclasses/mobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lifecycle orchestrator modules</name>
  <files>world/session_lifecycle.py, world/movement_lifecycle.py, world/death_lifecycle.py</files>
  <read_first>typeclasses/characters.py, typeclasses/mobs.py</read_first>
  <action>
Create three new orchestrator files per D-01/D-02. Each orchestrator exposes a single entry point that coordinates all system calls. All imports inside functions per D-04.

**world/session_lifecycle.py:**
- `on_login(character)` — extract the ENTIRE body of `Character.at_post_puppet` (lines 104-165 currently), starting from `init_session_accumulators` through `_send_new_player_guidance`. This includes:
  - init_session_accumulators call
  - stat_xp_accumulators init ({stat: 0.0 for stat in STAT_NAMES})
  - combat ndb state init (combat_handler=None, combat_target_id=None, active_effects=[], actions_remaining=0, ability_used_this_turn=False)
  - hp/stamina restore from db._saved_hp/db._saved_stamina with derive_max_hp/derive_max_stamina fallback, then clear saved values
  - charged_ability=None
  - SessionCommitScript creation check
  - oob_debounce={} init + push_status_update/push_stat_update/push_map_update/push_inventory_update
  - pending_quest_offer=None
  - ability_cooldowns={}, ancestry_ability_used=False, domain_resource=None
  - guild member domain resource init
  - start_regen call
  - new player guidance call (call character._send_new_player_guidance() since it does msg())
- `on_logout(character)` — extract the ENTIRE body of `Character.at_pre_unpuppet` (lines 206-252):
  - stop_regen call
  - save hp/stamina to db._saved_hp/db._saved_stamina
  - commit_stat_growth (note: currently called TWICE in source -- call only ONCE, this is a bug fix)
  - commit_session_xp
  - commit_skill_accumulators
  - on_member_disconnect
  - fishing cleanup (cancel bite/reel/idle deferreds, clear fishing_state)
  - crafting cleanup (cancel craft_deferred)
  - combat cleanup (remove_combatant, clear combat_handler)

**world/movement_lifecycle.py:**
- `on_move(character, source_location, **kwargs)` — extract the ENTIRE body of `Character.at_after_move` (lines 258-335):
  - cancel recovery on movement
  - break_stabilization_on_move
  - fishing state cleanup on move (cancel deferreds, msg "Fishing interrupted")
  - visited room tracking (visited_room_ids set update with copy-assign pattern)
  - flight guard (return early if in_flight)
  - push_map_update
  - room activity timestamp update
  - auto-engage aggressive mobs (start_combat + join_combat loop)
  - clear pending_quest_offer
  - fire_npc_reactive_echo
  - Resonance Sense passive check

**world/death_lifecycle.py:**
- `on_mob_death(mob, killer=None)` — extract the ENTIRE body of `SoravelonMob.at_death` (lines 154-223):
  - clear revealed_affixes, combat_scales
  - fire on_mob_death triggers
  - roll_loot + create_item_from_template loop
  - tome drop for named mobs
  - WorldEventLog entry for named mob death (check mob_id tag category="mob_id")
  - room state flags (blood_soaked, fading_life, power_vacuum)
  - check_kill_objectives for quest progress
  - schedule_respawn_from_death

All orchestrators: every import statement MUST be inside the function body (lazy import per D-04). No top-level imports from world/ modules.
  </action>
  <verify>
    <automated>python -c "from world.session_lifecycle import on_login, on_logout; from world.movement_lifecycle import on_move; from world.death_lifecycle import on_mob_death; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - world/session_lifecycle.py contains `def on_login(character):` and `def on_logout(character):`
    - world/movement_lifecycle.py contains `def on_move(character, source_location, **kwargs):`
    - world/death_lifecycle.py contains `def on_mob_death(mob, killer=None):`
    - No top-level `from world.` imports in any orchestrator file (all inside function bodies)
    - on_login contains `init_session_accumulators`, `push_status_update`, `start_regen`
    - on_logout contains `commit_session_xp`, `stop_regen`, `on_member_disconnect`
    - on_logout calls commit_stat_growth exactly ONCE (fix duplicate call bug)
    - on_move contains `push_map_update`, `start_combat`, `fire_npc_reactive_echo`
    - on_mob_death contains `roll_loot`, `schedule_respawn_from_death`, `WorldEventLog`
  </acceptance_criteria>
  <done>Three orchestrator modules exist with correct entry points and all logic extracted from typeclass hooks</done>
</task>

<task type="auto">
  <name>Task 2: Convert typeclass hooks to thin delegates</name>
  <files>typeclasses/characters.py, typeclasses/mobs.py</files>
  <read_first>typeclasses/characters.py, typeclasses/mobs.py, world/session_lifecycle.py, world/movement_lifecycle.py, world/death_lifecycle.py</read_first>
  <action>
Per D-03, replace the god method bodies with thin 3-line delegates.

**typeclasses/characters.py:**

Replace `at_post_puppet` body (after super() call) with:
```python
def at_post_puppet(self, **kwargs):
    super().at_post_puppet(**kwargs)
    from world.session_lifecycle import on_login
    on_login(self)
```

Replace `at_pre_unpuppet` body (before super() call at end) with:
```python
def at_pre_unpuppet(self):
    from world.session_lifecycle import on_logout
    on_logout(self)
    super().at_pre_unpuppet()
```

Replace `at_after_move` body (after super() call) with:
```python
def at_after_move(self, source_location, **kwargs):
    super().at_after_move(source_location, **kwargs)
    from world.movement_lifecycle import on_move
    on_move(self, source_location, **kwargs)
```

Keep `_send_new_player_guidance`, `at_look`, `at_before_move`, and all other methods unchanged.

**typeclasses/mobs.py:**

Replace `at_death` body with:
```python
def at_death(self, killer=None):
    from world.death_lifecycle import on_mob_death
    on_mob_death(self, killer)
```

Keep `ndb.revealed_affixes = set()` and `ndb.combat_scales = {}` clear in the orchestrator (already extracted in Task 1), NOT in the typeclass.

Keep all other mob methods unchanged. Keep the top-level imports of `apply_affixes_to_mob`, `get_star_prefix`, `MOB_AFFIXES` since they are used in other methods (not just at_death).

Run full test suite after changes.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && evennia test --settings server.conf.settings tests/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Character.at_post_puppet body is exactly: super() call + lazy import + on_login(self) (3 lines)
    - Character.at_pre_unpuppet body is exactly: lazy import + on_logout(self) + super() call (3 lines)
    - Character.at_after_move body is exactly: super() call + lazy import + on_move(self, source_location, **kwargs) (3 lines)
    - SoravelonMob.at_death body is exactly: lazy import + on_mob_death(self, killer) (2 lines)
    - No game logic remains in the typeclass hooks (only delegation)
    - `grep -c "from world.session_lifecycle" typeclasses/characters.py` returns 2 (at_post_puppet + at_pre_unpuppet)
    - `grep -c "from world.movement_lifecycle" typeclasses/characters.py` returns 1
    - `grep -c "from world.death_lifecycle" typeclasses/mobs.py` returns 1
    - Full test suite passes
  </acceptance_criteria>
  <done>All 4 typeclass hooks are thin delegates. All existing tests pass. No behavior change.</done>
</task>

</tasks>

<verification>
- `python -c "from world.session_lifecycle import on_login, on_logout"` succeeds
- `python -c "from world.movement_lifecycle import on_move"` succeeds
- `python -c "from world.death_lifecycle import on_mob_death"` succeeds
- Full test suite: `evennia test --settings server.conf.settings tests/`
- `grep -rn "from world.session_lifecycle\|from world.movement_lifecycle\|from world.death_lifecycle" typeclasses/` shows exactly 4 imports
</verification>

<success_criteria>
- 3 new orchestrator files exist in world/
- 4 typeclass hooks are thin delegates (3-line bodies)
- All orchestrator imports are lazy (inside function bodies)
- Full test suite passes with no regressions
- commit_stat_growth duplicate call bug fixed (called once in on_logout)
</success_criteria>

<output>
After completion, create `.planning/phases/16-architectural-refactoring/16-01-SUMMARY.md`
</output>
