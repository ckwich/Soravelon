---
phase: 16-architectural-refactoring
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - world/command_preprocessor.py
  - world/patrol_engine.py
  - world/node_helpers.py
  - world/zone_registry.py
  - typeclasses/mobs.py
autonomous: true
requirements: [D-13, D-14, D-15, D-16, D-17, D-18, D-19]

must_haves:
  truths:
    - "No SaverDict in-place mutations exist in production code"
    - "All public world/*.py functions return (bool, str) tuples (private functions exempt)"
    - "No unused imports in typeclasses/ or world/ modules"
    - "No top-level world/ imports in typeclasses/ files"
    - "No circular import paths between typeclasses/ and world/"
  artifacts:
    - path: "typeclasses/mobs.py"
      provides: "Lazy imports for mob_affix_roller and mob_affixes"
  key_links:
    - from: "typeclasses/mobs.py"
      to: "world/mob_affix_roller.py"
      via: "lazy import inside methods"
      pattern: "from world.mob_affix_roller import"
---

<objective>
Complete the SaverDict audit, return type enforcement, and import hygiene sweep per D-13 through D-19.

Purpose: Ensure codebase consistency with project conventions — SaverDict safety, (bool, str) return types, and clean imports. Research found these are mostly already clean, so this is a verification + fix pass.
Output: Small fixes across a few files, verified clean codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-architectural-refactoring/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SaverDict audit + Return type enforcement</name>
  <files>world/command_preprocessor.py, world/patrol_engine.py, world/node_helpers.py, world/zone_registry.py, world/nodes/node_effects.py</files>
  <read_first>world/command_preprocessor.py, world/patrol_engine.py, world/node_helpers.py, world/zone_registry.py, world/nodes/node_effects.py</read_first>
  <action>
**SaverDict Audit (D-13/D-14):**
Systematic grep for in-place mutations on db.* collections across ALL production files:

```bash
grep -rn "\.db\.\w*\[" world/*.py typeclasses/*.py commands/*.py
grep -rn "\.db\.\w*\.append\|\.db\.\w*\.add\|\.db\.\w*\.pop\|\.db\.\w*\.remove\|\.db\.\w*\.update\|del .*\.db\.\w*\[" world/*.py typeclasses/*.py commands/*.py
```

Research found zero violations, but verify. For any violations found, apply the copy-mutate-assign pattern:
```python
# BAD: db.foo[key] = val
# GOOD:
d = dict(character.db.foo)
d[key] = val
character.db.foo = d
```

Document all findings (even "zero violations confirmed") in the summary.

**Return Type Enforcement (D-15/D-16):**
Research identified 13 bare return/return None sites. Check each one:

1. `world/command_preprocessor.py` (2 sites, lines ~94, ~175) — These are parser functions. Check if they are PUBLIC (no underscore prefix). If public, they should return (bool, str). If they return parsed results (e.g., a string or None for "no match"), they may be exempt as data-returning functions, not action functions. Use judgment per D-16.

2. `world/patrol_engine.py` (1 site, line ~155) — fire-and-forget tick handler. If this is a private function or a Script callback, it's exempt per D-16.

3. `world/node_helpers.py` (1 site, line ~22) — lookup function returning None. If this is a public lookup that returns a found object or None, it's a data-returning function, not an action function. Likely exempt.

4. `world/zone_registry.py` (1 site, line ~20) — lookup returning None. Same assessment as node_helpers.

5. `world/nodes/node_effects.py` (1 site, line ~19) — tick handler. Likely exempt.

6. `world/scripts/patrol_script.py` (7 sites) — Script.at_repeat internal. Exempt per D-16 (internal/private).

For each site that IS a public action function (not a lookup/parser/callback), change:
```python
# Before
def some_public_function(...):
    ...
    return  # or return None

# After  
def some_public_function(...):
    ...
    return (True, "Action completed successfully.")
```

For lookup functions that return None for "not found", leave as-is — they return data, not action results. Document the reasoning for each decision.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -c "
import subprocess
result = subprocess.run(['grep', '-rn', r'\.db\.\w*\.append\|\.db\.\w*\.add\|\.db\.\w*\.pop\|del .*\.db\.\w*\[', 'world/', 'typeclasses/', 'commands/'], capture_output=True, text=True)
lines = [l for l in result.stdout.strip().split('\n') if l and '__pycache__' not in l and 'migrations/' not in l]
print(f'SaverDict violations: {len(lines)}')
for l in lines: print(l)
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - SaverDict grep returns 0 in-place mutation violations in production code
    - Each of the 13 bare return sites has been reviewed and either fixed or documented as exempt
    - No public action function in world/*.py returns None or bare return
    - Private functions (underscore prefix) and lookup/parser functions left as-is with documented reasoning
  </acceptance_criteria>
  <done>SaverDict audit complete (zero or fixed violations). Return types enforced on all applicable public functions.</done>
</task>

<task type="auto">
  <name>Task 2: Import hygiene — lazy imports + unused removal + circular check</name>
  <files>typeclasses/mobs.py</files>
  <read_first>typeclasses/mobs.py, typeclasses/characters.py, typeclasses/rooms.py, typeclasses/exits.py</read_first>
  <action>
**Move top-level world/ imports to lazy (D-17):**

Research found 2 top-level world/ imports in typeclasses/mobs.py (lines 9-10):
```python
from world.mob_affix_roller import apply_affixes_to_mob, get_star_prefix
from world.mob_affixes import MOB_AFFIXES
```

Move these inside the methods that use them:
- `spawn_with_affixes` and `initialize_for_spawn` use `apply_affixes_to_mob` — add lazy import inside each
- `get_display_name` uses `get_star_prefix` — add lazy import inside
- `reveal_affix` and `get_combat_modifiers` use `MOB_AFFIXES` — add lazy import inside each

Remove the top-level import lines 9-10.

**Remove unused imports (D-18):**
Run through each file in typeclasses/ and world/ and check for unused imports. Quick method:
```bash
python -m py_compile typeclasses/characters.py
python -m py_compile typeclasses/mobs.py
# etc.
```

For each file, check if all imported names are actually used. Remove any that aren't. Key files to check:
- typeclasses/characters.py (may have leftover imports after Plan 01 extraction)
- typeclasses/mobs.py (after lazy import conversion)
- world/*.py engine modules

NOTE: If Plan 01 (lifecycle orchestrators) has already run, characters.py will have had most imports moved to orchestrators. Check what remains and clean up any leftovers.

If Plan 01 has NOT run yet (parallel execution), only clean imports that are currently unused — don't anticipate future plan changes.

**Verify no circular imports (D-19):**
Run import verification:
```bash
python -c "import typeclasses.characters; import typeclasses.mobs; import typeclasses.rooms; import typeclasses.exits; print('No circular imports')"
```

Research confirmed the architecture is clean (typeclasses -> world/ is one-directional), but verify after all changes.

Run full test suite after all changes.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && evennia test --settings server.conf.settings tests/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^from world\." typeclasses/mobs.py` returns 0 lines (all moved to lazy)
    - `grep -n "^from world\." typeclasses/characters.py typeclasses/rooms.py typeclasses/exits.py` returns 0 lines from world/ at module level
    - `python -c "import typeclasses.characters; import typeclasses.mobs"` succeeds without circular import errors
    - Full test suite passes
  </acceptance_criteria>
  <done>All top-level world/ imports in typeclasses/ moved to lazy. Unused imports removed. No circular imports. Tests green.</done>
</task>

</tasks>

<verification>
- SaverDict grep finds 0 violations
- `grep -n "^from world\." typeclasses/*.py` returns 0 hits
- Import smoke test: `python -c "import typeclasses.characters; import typeclasses.mobs; import typeclasses.rooms"`
- Full test suite: `evennia test --settings server.conf.settings tests/`
</verification>

<success_criteria>
- SaverDict audit documented (expected: zero fixes needed)
- Return type enforcement applied to all applicable public functions
- All top-level world/ imports in typeclasses/ moved to lazy
- No unused imports remain
- No circular imports exist
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/16-architectural-refactoring/16-05-SUMMARY.md`
</output>
