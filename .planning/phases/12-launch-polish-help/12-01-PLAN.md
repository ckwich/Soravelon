---
phase: 12-launch-polish-help
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/conf/connection_screens.py
  - world/ancestry_engine.py
  - typeclasses/characters.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "Connecting player sees dark fantasy atmospheric text before login"
    - "New player choosing ancestry receives thematically correct starter gear in inventory"
    - "New player logging in without ancestry sees guided prompt to type ancestry"
    - "New player with ancestry but no guild sees hint to explore for guild discovery"
  artifacts:
    - path: "server/conf/connection_screens.py"
      provides: "Dark fantasy connection screen"
      contains: "CONNECTION_SCREEN"
    - path: "world/ancestry_engine.py"
      provides: "Starter kit creation in set_ancestry()"
      contains: "create_item_from_template"
    - path: "typeclasses/characters.py"
      provides: "New player guidance prompts in at_post_puppet()"
      contains: "ancestry is None"
  key_links:
    - from: "world/ancestry_engine.py"
      to: "world/item_spawner.py"
      via: "create_item_from_template for starter gear"
      pattern: "create_item_from_template"
    - from: "typeclasses/characters.py"
      to: "at_post_puppet"
      via: "ancestry/guild None checks"
      pattern: "self\\.db\\.ancestry.*None"
---

<objective>
New player first experience: atmospheric connection screen and guided onboarding.

Purpose: The connection screen sets dark fantasy tone before login. After creation, guided prompts steer players through ancestry choice and guild discovery. Ancestry-based starter kits ensure players have gear appropriate to their chosen ancestry.

Output: Updated connection screen, starter kit wiring in ancestry engine, new player guidance in character hooks.
</objective>

<execution_context>
@C:\Users\colek\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\colek\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-launch-polish-help/12-CONTEXT.md
@server/conf/connection_screens.py
@world/ancestry_engine.py
@typeclasses/characters.py
@world/item_spawner.py
@world/areas/equipment_catalog.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From world/item_spawner.py:
```python
def create_item_from_template(item_def, location=None):
    """Create an Evennia item object from a definition dict."""
```

From world/ancestry_engine.py:
```python
ANCESTRY_TRAITS = {
    "human": {"name": "Human", ...},
    "kauroran": {"name": "Kau'roran", ...},
    "veth": {"name": "Veth", ...},
    "selvar": {"name": "Selvar", ...},
}
def set_ancestry(character, ancestry_id, coat=None):
    """Set ancestry for a character during character creation. Returns (bool, str)."""
```

Equipment catalog item_ids (from equipment_catalog.py):
- iron_sword, iron_dagger, iron_staff, iron_bow, iron_spear
- leather_armor, hide_armor, cloth_robes
- healing_potion (need to verify this exists or create inline)

From typeclasses/characters.py at_post_puppet:
```python
def at_post_puppet(self, **kwargs):
    # OOB pushes, session accumulators, combat state init...
    # New player guidance goes AFTER existing init code
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connection screen + starter kit in set_ancestry</name>
  <files>server/conf/connection_screens.py, world/ancestry_engine.py</files>
  <action>
**Connection screen (per D-01, D-02):**
Replace the default CONNECTION_SCREEN in `server/conf/connection_screens.py` with a dark fantasy atmospheric version. Keep the same variable name and format string pattern (SERVERNAME, version). Use Evennia ANSI color codes (|r, |c, |w, |n, |x, |m, etc). Structure:
1. Atmospheric lore excerpt (2-3 lines, mysterious/dark fantasy — e.g., "The old patterns stir beneath the stone...")
2. Game name (Soravelon) in prominent color
3. Clear connect/create instructions (same format as original)
4. "Enter |whelp|n for more info." line
No ASCII art — just evocative text with color. Keep it under 15 lines total.

**Starter kit in set_ancestry (per D-10, D-11):**
In `world/ancestry_engine.py`, add a `STARTER_KITS` dict mapping ancestry_id to list of item template dicts. Each dict has the fields item_spawner expects (item_id, key, item_type, weight, rarity, equip_slot, desc, value, plus damage_min/damage_max/stat_bonuses for weapons).

Starter kits per D-10:
- human: iron_sword, leather_armor, 50 Scales, 2x healing_potion
- kauroran: iron_spear, hide_armor, 50 Scales, 2x healing_potion
- veth: iron_dagger (x2), leather_armor, 50 Scales, 2x healing_potion, lockpick
- selvar: iron_staff, cloth_robes, 50 Scales, 2x healing_potion

For items that exist in equipment_catalog, look up the zone_obj for equipment_catalog (search_tag "equipment_catalog" category "zone_id") and find the item_def. For healing potions, define inline dicts: `{"item_id": "healing_potion", "key": "Healing Potion", "item_type": "item", "weight": 0.3, "rarity": "normal", "value": 10, "desc": "A small vial of crimson liquid that restores health.", "heal_amount": 30}`.

For the lockpick, define inline: `{"item_id": "lockpick", "key": "Lockpick", "item_type": "item", "weight": 0.1, "rarity": "normal", "value": 5, "desc": "A slender tool for working locks."}`.

In `set_ancestry()`, AFTER the existing `_apply_starting_standings` and `apply_ancestry_skill_seeds` calls, add starter kit creation:
1. Look up equipment_catalog zone_obj via `evennia.search_tag("equipment_catalog", category="zone_id")`
2. For each item in STARTER_KITS[ancestry_id]: if item has an equipment_catalog item_id, find it in zone_obj.db.item_definitions; otherwise use the inline dict
3. Call `create_item_from_template(item_def, location=character)` for each item
4. Set `character.db.carried_scales = 50` (the Scales currency)
5. Import create_item_from_template from world.item_spawner (lazy import inside set_ancestry to avoid circular imports)

Handle the case where equipment_catalog zone_obj might not exist (server not fully loaded) — log a warning and skip items rather than crashing.
  </action>
  <verify>
    <automated>cd /c/Dev/Evennia/soravelon && python -c "from server.conf.connection_screens import CONNECTION_SCREEN; assert 'connect' in CONNECTION_SCREEN.lower(); assert 'create' in CONNECTION_SCREEN.lower(); assert len(CONNECTION_SCREEN) > 100; print('Connection screen OK')" && python -c "from world.ancestry_engine import STARTER_KITS; assert len(STARTER_KITS) == 4; assert all(k in STARTER_KITS for k in ['human','kauroran','veth','selvar']); print('Starter kits OK')"</automated>
  </verify>
  <done>Connection screen displays dark fantasy atmospheric text with connect/create instructions. STARTER_KITS dict exists with all 4 ancestries. set_ancestry() creates starter items and sets 50 carried_scales after ancestry choice.</done>
</task>

<task type="auto">
  <name>Task 2: New player guidance prompts in at_post_puppet</name>
  <files>typeclasses/characters.py</files>
  <action>
**New player guidance (per D-09):**
In `typeclasses/characters.py`, add guidance prompts at the END of `at_post_puppet()` (after all existing init code). The logic:

1. Check `self.db.ancestry is None`:
   - If True, send: `"|y[New Player]|n You must first choose your ancestry. Type |wancestry|n to begin."`
   - Return early (no further guidance needed yet)

2. Check `self.db.guild_id is None`:
   - If True, send: `"|y[Hint]|n Your actions shape which guilds take notice of you. Explore the world and practice your skills — a guild may discover you when you have proven yourself."`

3. Check if character has an active quest (self.db.active_quest_ids is empty or doesn't exist):
   - If no active quests AND ancestry is set AND guild_id is set, send: `"|y[Hint]|n Speak with the townsfolk in Vael's Crossing. Type |wtalk|n near an NPC to begin a conversation."`

Use `self.msg()` for all messages. Add a brief delay feel by putting a blank line between the standard login output and guidance: `self.msg("")` before the guidance block.

Guard all guidance behind a `_send_new_player_guidance(self)` private method on Character for cleanliness. Call it at the end of at_post_puppet.

Do NOT add guidance if character has been playing a while — use a simple heuristic: if `self.db.backend_level > 1` or `len(self.db.domain_scores or {}) > 2`, skip all guidance (player is experienced).
  </action>
  <verify>
    <automated>cd /c/Dev/Evennia/soravelon && python -c "import ast; tree = ast.parse(open('typeclasses/characters.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]; assert '_send_new_player_guidance' in funcs; print('Guidance method exists')" && grep -c "ancestry.*None" typeclasses/characters.py</automated>
  </verify>
  <done>New characters see ancestry prompt on first login. Characters with ancestry but no guild see exploration hint. Experienced players (backend_level > 1 or 2+ domain scores) see no guidance. All prompts use colored Evennia ANSI formatting.</done>
</task>

</tasks>

<verification>
- Connection screen: `python -c "from server.conf.connection_screens import CONNECTION_SCREEN; print(CONNECTION_SCREEN)"` displays atmospheric dark fantasy text with connect/create instructions
- Starter kits: `python -c "from world.ancestry_engine import STARTER_KITS; print(list(STARTER_KITS.keys()))"` shows all 4 ancestries
- Guidance: `grep -n "new_player_guidance\|ancestry.*None" typeclasses/characters.py` shows guidance method and ancestry check
</verification>

<success_criteria>
1. Connection screen sets dark fantasy tone with connect/create instructions (D-01, D-02)
2. Each ancestry gets correct starter gear via set_ancestry (D-10, D-11)
3. New players see guided prompts for ancestry, guild, and NPC interaction (D-09)
4. Experienced players see no guidance
</success_criteria>

<output>
After completion, create `.planning/phases/12-launch-polish-help/12-01-SUMMARY.md`
</output>
