---
phase: 03.1-mob-spawn-runtime
plan: 05
type: execute
wave: 3
depends_on: [03.1-03, 03.1-04]
files_modified:
  - tests/test_mob_spawner.py
  - tests/test_item_spawner.py
  - tests/test_loot_tables.py
  - tests/test_area_builder.py
autonomous: true
requirements: [SPAWN-01, SPAWN-02, SPAWN-03, SPAWN-04, SPAWN-05, SPAWN-06, SPAWN-07, SPAWN-08, SPAWN-09, SPAWN-10, SPAWN-11, SPAWN-12, SPAWN-13, SPAWN-14]

must_haves:
  truths:
    - "All test files import without error (no DB required for pure-logic tests)"
    - "test_mob_spawner.py covers: count management, reload preservation, named mob tag, patrol attachment, respawn guard at count_max"
    - "test_item_spawner.py covers: typeclass selection per item_type, db attr mapping, extra attrs passed through"
    - "test_loot_tables.py covers: tier mapping by skill, drop chance, extra_rolls by rarity, zone override, missing mob_type"
    - "test_area_builder.py covers: area.item() stores on zone_obj.db.item_definitions; area.named_mob() produces spawn_def with is_named=True on room.db.spawn_definitions"
    - "named_mob_definitions attr NOT initialized on rooms (area_builder refactor verified)"
  artifacts:
    - path: "tests/test_mob_spawner.py"
      provides: "mob spawner test suite"
    - path: "tests/test_item_spawner.py"
      provides: "item spawner test suite"
    - path: "tests/test_loot_tables.py"
      provides: "loot table test suite"
    - path: "tests/test_area_builder.py"
      provides: "area builder regression + new item/named_mob tests"
  key_links:
    - from: "tests/test_mob_spawner.py"
      to: "world/mob_spawner.py"
      via: "sys.modules injection for evennia + MagicMock stubs"
      pattern: "sys.modules"
    - from: "tests/test_loot_tables.py"
      to: "world/loot_tables.py"
      via: "MagicMock mob.db.mob_type, killer.db.domain_scores"
      pattern: "MagicMock"
---

<objective>
Consolidate and complete the full test suite for Phase 3.1. This plan verifies that all previously written test scaffolds from Plans 01-04 are complete, covers any gaps, and adds area_builder regression tests for the named_mob refactor and item() method.

Purpose: Confidence that the spawn runtime works end-to-end without a live Evennia server. Tests are the only automated verification available before content is authored.
Output: Four test files all passing, covering all D-* requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-mob-spawn-runtime/03.1-CONTEXT.md
@.planning/phases/03.1-mob-spawn-runtime/03.1-01-SUMMARY.md
@.planning/phases/03.1-mob-spawn-runtime/03.1-02-SUMMARY.md
@.planning/phases/03.1-mob-spawn-runtime/03.1-03-SUMMARY.md
@.planning/phases/03.1-mob-spawn-runtime/03.1-04-SUMMARY.md

<interfaces>
<!-- Prior plan outputs to verify against. -->

world/mob_spawner.py public API:
  spawn_zone(zone_obj), spawn_room_mobs(room), spawn_single_mob(spawn_def, room),
  spawn_named_mob(spawn_def, room, is_respawn=False), _schedule_respawn(spawn_def, room),
  _count_room_mobs(room, spawn_def), _maybe_attach_patrol(mob, spawn_def, room)

world/item_spawner.py public API:
  create_item_from_template(item_def, location=None)

world/loot_tables.py public API:
  roll_loot(mob, killer), get_loot_modifiers(mob), LOOT_TABLES dict

world/area_builder.py new API:
  area.item(item_id, **kwargs) — stored on zone_obj.db.item_definitions
  area.named_mob(mob_id, room, **kwargs) — now calls spawn() with is_named=True
  spawn() dict includes is_named, prestige_modifier, tome_drop, spawn_condition, sequence

Test conventions from prior phases:
  - Pure logic tests: unittest.TestCase + sys.modules injection for evennia
  - MagicMock for mob, room, character objects
  - NO EvenniaTestCase for pure-logic modules (avoids SQLite lock)
  - See tests/test_world_state.py and tests/test_area_builder.py for patterns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete test_mob_spawner.py and test_item_spawner.py</name>
  <files>tests/test_mob_spawner.py, tests/test_item_spawner.py</files>
  <read_first>
    - tests/test_mob_spawner.py (if it exists from Plan 01 TDD — check content for gaps)
    - tests/test_item_spawner.py (if it exists from Plan 01/02 TDD — check content for gaps)
    - world/mob_spawner.py (read to understand actual implementation for accurate test assertions)
    - world/item_spawner.py (read to understand actual implementation)
    - tests/test_world_state.py (sys.modules injection pattern reference)
  </read_first>
  <action>
**test_mob_spawner.py — ensure these test cases exist and pass:**

sys.modules injection at top before import:
```python
import sys
import unittest
from unittest.mock import MagicMock, patch

# Inject minimal evennia stub before import
evennia_mock = MagicMock()
sys.modules['evennia'] = evennia_mock
sys.modules['typeclasses'] = MagicMock()
sys.modules['typeclasses.mobs'] = MagicMock()
sys.modules['world.zone_scaling'] = MagicMock()
sys.modules['world.scripts.patrol_script'] = MagicMock()
sys.modules['twisted.internet'] = MagicMock()
```

Required test classes:

`TestCountRoomMobs`:
- test_counts_matching_mobs: room with 2 matching SoravelonMob contents → count=2
- test_excludes_non_matching: mob.key="other_mob" → count=0
- test_named_mob_uses_tag_search: is_named=True → uses evennia.search_tag with category="mob_id"

`TestSpawnRoomMobs`:
- test_spawns_to_count_min: count_min=2, existing=0 → spawn_single_mob called 2 times
- test_preserves_existing: count_min=2, existing=2 → spawn_single_mob not called (D-03)
- test_partial_fill: count_min=2, existing=1 → spawn_single_mob called 1 time
- test_empty_spawn_definitions: room.db.spawn_definitions=[] → returns 0

`TestSpawnSingleMob`:
- test_sets_zone_id: created mob.db.zone_id == room.db.zone_id
- test_calls_initialize_for_spawn: mob.initialize_for_spawn called with room
- test_sets_base_disposition: mob.db.base_disposition set from spawn_def

`TestSpawnNamedMob`:
- test_tags_mob_with_mob_id: mob.tags.add called with (mob_key, category="mob_id")
- test_sets_prestige_modifier: mob.db.prestige_modifier set from spawn_def
- test_sets_tome_drop: mob.db.tome_drop set from spawn_def
- test_respawn_announcement: is_respawn=True → room.msg_contents called

`TestScheduleRespawn`:
- test_calls_callLater: twisted.reactor.callLater called with delay > 0
- test_no_spawn_at_count_max: callback fires, count >= count_max → spawn_single_mob not called

**test_item_spawner.py — ensure these test cases exist and pass:**

```python
sys.modules injection for evennia
```

`TestCreateItemFromTemplate`:
- test_equipment_typeclass: item_type="equipment" → typeclass "typeclasses.objects.SoravelonEquipment"
- test_container_typeclass: item_type="container" → typeclass "typeclasses.objects.SoravelonContainer"
- test_default_typeclass: item_type="item" → SoravelonItem; missing item_type → SoravelonItem
- test_sets_weight: item.db.weight == item_def["weight"]
- test_sets_rarity: item.db.rarity == item_def["rarity"]
- test_sets_equip_slot: item_type="equipment", equip_slot="weapon" → item.db.equip_slot=="weapon"
- test_extra_attrs_passed: damage_min=5 in item_def → item.db.damage_min == 5
- test_reserved_keys_not_extra: item_id, key, item_type not set as extra db attrs (not double-set)
- test_location_set: location=mock_room → evennia.create_object called with location=mock_room
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -m pytest tests/test_mob_spawner.py tests/test_item_spawner.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_mob_spawner.py -q → all pass, no errors
    - python -m pytest tests/test_item_spawner.py -q → all pass, no errors
    - grep -c "def test_" tests/test_mob_spawner.py  # at least 12
    - grep -c "def test_" tests/test_item_spawner.py  # at least 9
  </acceptance_criteria>
  <done>Both test files exist with full coverage; all tests pass; no EvenniaTestCase used — pure unittest.TestCase with MagicMock</done>
</task>

<task type="auto">
  <name>Task 2: test_loot_tables.py + area_builder regression tests</name>
  <files>tests/test_loot_tables.py, tests/test_area_builder.py</files>
  <read_first>
    - tests/test_loot_tables.py (if exists from Plan 04 TDD — check gaps)
    - tests/test_area_builder.py (existing file — read to understand current test structure before adding cases)
    - world/loot_tables.py (read LOOT_TABLES structure and roll_loot implementation)
    - world/area_builder.py (read item() method, named_mob() wrapper, spawn() dict for assertions)
  </read_first>
  <action>
**test_loot_tables.py — ensure these test cases exist and pass:**

No Evennia DB needed — all mocks. sys.modules inject world.zone_scaling with get_material_tier stub.

`TestRollLoot`:
- test_returns_empty_for_unknown_mob_type: mob.db.mob_type="dragon" → [] (not in LOOT_TABLES)
- test_skill_0_gives_tier_1: killer skill 0 → tier 1 → item_def rarity == rarity_by_tier[0]
- test_skill_60_gives_tier_3: killer skill 60 → tier 3 → item_def rarity == rarity_by_tier[2]
- test_skill_95_gives_tier_5: killer skill 95 → tier 5 → item_def rarity == rarity_by_tier[4]
- test_drop_chance_0_returns_empty: patch base_drop_chance=0.0 → always []
- test_drop_chance_1_returns_item: patch base_drop_chance=1.0, random.random=0.0 → non-empty
- test_extra_rolls_legendary: mob.db.rarity="legendary" (extra_rolls=2) → up to 3 items
- test_zone_override_used: zone_obj.db.loot_table_overrides has entry for mob_type → override used instead of LOOT_TABLES
- test_no_killer_defaults_tier_1: killer=None handled gracefully → tier 1 (or empty)
- test_item_def_has_required_keys: result dicts contain item_id, key, item_type, weight, rarity, value, desc

`TestGetLootModifiers` (regression — existing behavior):
- test_normal_rarity: extra_rolls=0
- test_legendary_rarity: extra_rolls=2, tome_chance=0.15

**test_area_builder.py — add new test methods to existing test class (or new class):**

Read the existing tests first to avoid duplication. Add to the existing test file:

`TestAreaBuilderItemMethod` (new class if doesn't exist):
- test_item_stores_on_zone_obj: area.item("sword_01", key="iron sword", item_type="equipment", weight=2.0, rarity="normal", equip_slot="weapon", desc="A basic sword.", value=10) → zone_obj.db.item_definitions has one entry with item_id="sword_01"
- test_item_chaining: area.item("a").item("b") → zone_obj.db.item_definitions has 2 entries
- test_item_requires_zone: area.item("x") without calling zone() first → raises AreaBuilderValidationError

`TestAreaBuilderNamedMobRefactor` (new class):
- test_named_mob_creates_spawn_def: area.named_mob("boss_wolf", room) → room.db.spawn_definitions has one entry with is_named=True, mob="boss_wolf"
- test_named_mob_no_named_mob_definitions: after area.named_mob(), room.db.named_mob_definitions does NOT exist (or is empty/None) — the attr should not be set by _set_room_attrs
- test_named_mob_respawn_minutes: area.named_mob("boss", room, respawn_minutes=240) → spawn_def["respawn_minutes"]==240
- test_named_mob_tome_drop: area.named_mob("boss", room, tome_drop="ancient_tome") → spawn_def["tome_drop"]=="ancient_tome"

Note: test_area_builder.py likely uses EvenniaTestCase or has its own mock setup. Read the existing file fully before adding tests — match the existing setUp pattern.
  </action>
  <verify>
    <automated>cd C:/Dev/Evennia/soravelon && python -m pytest tests/test_loot_tables.py tests/test_area_builder.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_loot_tables.py -q → all pass
    - python -m pytest tests/test_area_builder.py -q → all pass (no regressions)
    - grep -c "def test_" tests/test_loot_tables.py  # at least 10
    - grep -n "TestAreaBuilderItemMethod\|TestAreaBuilderNamedMobRefactor\|test_named_mob_creates_spawn_def\|test_item_stores_on_zone_obj" tests/test_area_builder.py
  </acceptance_criteria>
  <done>test_loot_tables.py covers tier mapping, drop chance, extra rolls, zone override; test_area_builder.py covers item() and named_mob() refactor without breaking existing tests; all tests pass</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_mob_spawner.py tests/test_item_spawner.py tests/test_loot_tables.py tests/test_area_builder.py -q → all green
- No EvenniaTestCase used in test_mob_spawner.py, test_item_spawner.py, test_loot_tables.py
- Total test count: test_mob_spawner.py ≥12, test_item_spawner.py ≥9, test_loot_tables.py ≥10
</verification>

<success_criteria>
Full test suite for Phase 3.1 passing. Every D-* requirement from CONTEXT.md has at least one test case verifying it. named_mob_registry deletion verified via area_builder tests. Skill-based loot tier verified by test_loot_tables.py.
</success_criteria>

<output>
After completion, create .planning/phases/03.1-mob-spawn-runtime/03.1-05-SUMMARY.md
</output>
